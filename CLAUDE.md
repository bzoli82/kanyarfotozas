# KanyarFotózás Platform

## Tech stack
- Backend: Laravel 13 (PHP 8.4), Sanctum-kompatibilis session auth, Spatie Permission, Spatie ActivityLog
- Frontend: Vue.js 3 + Inertia.js, Tailwind CSS 4, Leaflet.js
- DB: PostgreSQL 18 + PostGIS (natívan telepítve, nem Dockerben)
- Storage: fejlesztésben lokális `local`/`public` disk + `nas` (SFTP) — élesben Cloudflare R2 (2 bucket: `r2_public`/`r2_private`). A disk-választás env-vezérelt: `MEDIA_PUBLIC_DISK` / `MEDIA_ARCHIVE_DISK` (`config/media.php` -> `disks`, `App\Services\MediaStorage`), a kód nem tartalmaz hardcode-olt disknevet. Feltöltés + feldolgozás MINDIG a lokális `local` stagingen (valós fájl-útvonal kell); utána a kis publikus fájlok a `public()`, a nagy fájlok (eredeti + letölthető) az archiváló job-bal az `archive()` diskre. Frontend: `mediaBaseUrl` Inertia shared prop + `useMediaUrl()` composable (`mediaUrl(key)`), nincs hardcode-olt `/storage/`. Átállás: `php artisan kanyarfotozas:sync-media-storage`.
- Képfeldolgozás (EPIC-04, kész): Intervention Image – WebP thumbnail + vízjel + Gaussian blur
- Videófeldolgozás (EPIC-05, kész): FFmpeg (Symfony/Illuminate Process) – 720p H.264 preview, scrub sprite sheet, vízjel
- Fizetés (EPIC-07, kész — MVP): Stripe Checkout (sandbox), SimplePay/Barion később
- Design rendszer: `resources/css/app.css` CSS custom property-k (`--color-surface-0/1/2`, `--color-accent` stb.) — Stílus 4 "parallax hero", sötét (#0d0d0d), piros akcent (#e63946)

## Szerepkörök
superadmin > admin > photographer (Spatie Permission + `users.role` enum kettős forrásból). Photographer csak saját `photographer_id` adatait látja/kezelheti.

## Fejlesztési állapot (epic-enként, ld. docs/files_extracted/master.txt a teljes specihez)
- EPIC-01 (infra) és EPIC-02 (adatmodell) kész
- EPIC-03 (admin auth + esemény/média CRUD) kész — login session-alapú (`/login`), `role:` middleware, `Admin\EventController`, `Admin\MediaController`. Fotós is létrehozhat eseményt, de csak a sajátját szerkesztheti/törölheti (`manage-event` Gate). **Az esemény szerkesztő oldal + a média-kezelő EGY oldal** (`Admin/Events/Show.vue`, `/admin/events/{event}`): felül az „Esemény adatai" űrlap (`PUT /admin/events/{event}` + „Esemény törlése"), alatta a média feltöltés + FTP-import + a média-rács (elrejtés/törlés per kép, az ár csak KIÍRVA). **Több média egyszerre**: a rácsban kattintással VAGY drag-kerettel (rubber-band, `pointerdown`/`move`/`up` a grid konténeren, `getBoundingClientRect` metszés) jelölhető ki több elem → sticky művelet-sáv → `POST /admin/events/{event}/media/bulk-delete` (`MediaController::bulkDestroy`, `ids[]` + `delete_ftp_source`); ha bármely kijelölt média FTP-importból jött, egy checkbox felajánlja az **eredeti forrás-fájl** törlését is a távoli szerverről. NINCS külön `/edit` route/oldal — a listában a név ÉS a „Szerkesztés" link is ide mutat. `Admin/Events/Form.vue` CSAK az új-esemény létrehozáshoz van (`GET /admin/events/create` → `POST /admin/events`). `UpdateEventRequest::rules()` az `import_*` mezőket kiveszi (azok csak létrehozáskor élnek).
  - **Esemény-szintű árazás**: `events.photo_price_cents` + `events.video_price_cents` (nullable → `base_price_huf` fallback, `Event::priceFor($type)`). Az űrlapon (create ÉS a szerkesztő oldalon) „Fotó ára" / „Videó ára" mező, alap = `base_price_huf` (`basePrice` prop). Új média ára a `MediaIngestor` az esemény árából veszi (nem fájlonként). Az ár módosításakor `EventController::update` a `$event->wasChanged(...)` alapján tömegesen átárazza a meglévő (még el nem adott) médiát; a már megvásárolt tétel ára az `order_media`-ban rögzül, változatlan. `MediaController::update` már CSAK a láthatóságot állítja (`price_cents` szabály törölve).
  - **Duplikátum-szűrés**: `media.content_hash` (a feltöltött/importált fájl SHA-256 lenyomata, index `(event_id, content_hash)`). Webes feltöltés (`MediaController::store`) és FTP-import (`FtpImport`) is a közös `App\Services\MediaIngestor::ingestStaged()`-en át megy: ha egy eseményben már van azonos tartalmú fájl, kihagyja (a staging fájlt törli), a flash üzenet jelzi hány duplikátumot hagytunk ki. Eseményenként — ugyanaz a fájl más eseménybe bekerülhet.
  - **Média-törlés**: `App\Services\MediaDeleter::delete($media, $deleteImportSource)` — az összes legyártott fájlt törli (eredeti + letölthető JPEG/WebP az archív/staging diskről; thumbnail + vízjeles előnézet + scrub-sprite + **HLS playlist és a `hls/{id}/` könyvtár** a publikus diskről), majd a DB rekordot; `$deleteImportSource` esetén az FTP-forrás fájlt is a `nas` disken. Minden fájlművelet `rescue(report: false)`-ban (a `nas` disk `throw`-ol hiányzó fájlnál). Az egyes törlés (`MediaController::destroy`) és a köteges (`bulkDestroy`) is ezt hívja.
- EPIC-04 (képfeldolgozó pipeline) kész — `App\Services\ImageProcessingService` (Intervention Image v4, GD driver, nincs Imagick) + `App\Jobs\ProcessImageMedia`: 400x300 WebP thumbnail (`public` disk), 1200px WebP 80% csempézett vízjellel (`public` disk, betűtípus: `resources/fonts/Arimo-Bold.ttf`, Apache-2.0), letölthető JPEG 92% + WebP 90% eredeti felbontásban (`local` disk, privát, utána NAS-ra archiválva). Sikertelen feldolgozás 3x retry után `status=failed`.
- A média eredeti fájlja a `local` (privát) diskre kerül `originals/{event_id}/{uuid}.{ext}` néven; képnél a `ProcessImageMedia`, videónál a `ProcessVideoMedia` job dolgozza fel, utána mindkettő elindítja az `ArchiveMediaOriginalToNas` jobot.
- EPIC-05 (videófeldolgozó pipeline) kész — `App\Services\VideoProcessingService` (FFmpeg a `Illuminate\Support\Facades\Process` fasade-on át, bináris útvonal: `config('media.ffmpeg_binary')`/`ffprobe_binary`, lásd `.env` `FFMPEG_BINARY`/`FFPROBE_BINARY` — erre a gépre expliciten ki kellett tölteni, mert a PATH nem frissült a shell-ekben a winget telepítés után) + `App\Jobs\ProcessVideoMedia` (a `videos` queue-ra kerül, prioritásos feldolgozáshoz `queue:work --queue=videos,default`): thumbnail (első másodperc kockájából, `ImageProcessingService::makeThumbnail` újrafelhasználva), 720p H.264/AAC vízjelezett előnézet (max 2 Mbps, ugyanaz a csempézett vízjel logika mint a képeknél, PNG overlay-ként ráégetve), scrub sprite (2 mp-enként 160x90 WebP/PNG frame, egy sprite sheet-be csempézve — nincs külön oszlop/sor DB mező, a frontend a sprite kép saját méretéből számolja ki). Nem-MP4 (MOV/AVI) eredetit MP4-re remuxol (előbb stream copy, ha nem megy, újrakódolás) — a Media modellben nincs külön "letölthető videó" mező, az `original_s3_key` szolgál letölthető forrásként is.
- Teszt-videó fixture: `tests/Fixtures/sample.mp4` és `sample.mov` (FFmpeg `testsrc`/`sine` lavfi generátorral létrehozott, 6 másodperces szintetikus klip — nem valódi felvétel, csak a pipeline teszteléséhez).
- Vízjel testreszabás (superadmin, `/admin/settings/watermark`) kész — `App\Services\WatermarkSettings` (szöveg/betűtípus/méret/sűrűség, `site_settings` táblában tárolva) közös forrás mind az `ImageProcessingService`, mind a `VideoProcessingService` számára. 3 bundle-olt font (`resources/fonts/`: Arimo-Bold, DejaVuSans-Bold, DejaVuSerif-Bold — mind szabadon terjeszthető). Az admin oldalon élő előnézet van (`ImageProcessingService::previewWithSettings()` egy statikus minta-képen, `public/images/watermark-preview-sample.jpg`), ami a MÉG EL NEM MENTETT form-értékeket mutatja.
- Tesztadatbázis: **PostgreSQL** (`kanyarfotozas_test`, PostGIS-szel), NEM sqlite — az `events` tábla egyik migrációja natív PostGIS `DB::statement` spatial indexet hoz létre, ami sqlite-on elhasal
- Világos/sötét/rendszer témaváltó + admin téma testreszabás kész — `App\Services\ThemeSettings` (`site_settings`: `theme_preset` / `theme_mode` / `accent_color` / `border_radius` / `font_family` / `theme_custom` JSON, akcent-hover automatikusan sötétített szín) az `/admin/settings/theme` (csak superadmin) oldalon szerkeszthető, sémaválasztóval + élő világos/sötét előnézettel.
  - **Színsémák** (`ThemeSettings::PRESETS`): mindegyik teljes **világos + sötét paletta** (`surface_0/1/2`, `border`, `content`, `muted`) + ajánlott akcentszín. `default` = „Aszfalt" (a jelenlegi sötét #0d0d0d / piros #e63946 megjelenés) — **ez az alapértelmezés**. Továbbiak: `racing` (borostyán), `ocean` (kék), `forest` (zöld), `grape` (lila), `slate` (indigó). A `custom` sémánál minden szín kézzel állítható mindkét módhoz (`theme_custom` JSON). Séma kiválasztásakor a Vue űrlap előtölti az akcentszínt + a custom-szerkesztőt a sémából (finomhangolható).
  - **Injektálás**: `ThemeSettings::resolvedPalettes()` → az `app.blade.php` inline `<style>`-je adja meg mind a világos (`:root, :root[data-theme='light']`), mind a sötét (`:root[data-theme='dark']` + `@media (prefers-color-scheme: dark) :root:not([data-theme='light'])` a JS-mentes esetre) palettát CSS custom property-ként (`--color-surface-*` / `--color-border` / `--color-content` / `--color-muted`), plusz az `--color-accent` / `--color-accent-hover` / `--radius-base` / `--font-sans-base`. **FONTOS**: ez a `<style>` az UTOLSÓ elem a `<head>`-ben, és **azonos szelektorokat** használ, mint a `resources/css/app.css` alapértelmezései → forrássorrend alapján mindig a blade nyer (az app.css `:root[data-theme=...]` blokkjai csak fallback-ként maradnak, a `default` séma értékeivel). Böngészőben ellenőrizve: séma-váltás + világos/sötét mód is helyesen alkalmazza a palettát.
  - A látogató a fejléc `ThemeToggle.vue` gombjával választhat világos/sötét/rendszer módot (Pinia `resources/js/Stores/theme.js`, localStorage `kanyarfotozas.theme`, FOUC ellen `app.blade.php` szinkron `<script>` állítja a `data-theme`-et lefestés előtt) — ez csak az ő módja, a séma színei mindkét módban érvényesek.
  335 PHPUnit teszt zöld.
- EPIC-06 (publikus galéria + keresés + videó UX) kész — `/events` (lista, `App\Services\EventSearch`: helyszín/ország/dátum/típus szűrés + GPS sugaras kereséskor PostGIS `ST_DWithin`/`ST_Distance`), `/events/{event:slug}` (esemény galéria, foto/videó kártyák, videónál hover-play + scrub bar a sprite-ból), `/media/{media}` (részletes nézet + "közeli" média ugyanabból az eseményből ±3 percen belül), `/cart` (Pinia store `resources/js/Stores/cart.js`, localStorage-perzisztált, `/cart` betöltéskor `GET /api/cart?ids[]=...`-vel élő adatokkal szinkronizálva — elavult/törölt/elrejtett média csendben kikerül, ár frissül). Hibrid minta: első oldal Inertia (SEO), további oldalak `GET /api/events/{event}/media` JSON API-n át (IntersectionObserver infinite scroll). `App\Http\Resources\MediaResource` az egyetlen publikus-biztonságos média reprezentáció (soha nem exponálja az `original_s3_key`/download mezőket).

- EPIC-07 (vásárlás + fizetés) kész — Stripe Checkout (sandbox), kuponkód, letöltési token, JPEG/WebP/MP4 + ZIP letöltés, e-mail visszaigazolás. `App\Services\CouponService` (kedvezmény-számítás/validálás, `/api/coupon/validate` élő előnézet a kosár oldalon), `App\Services\CheckoutService` (`createPendingOrder()` — média-validálás + kupon + rendelés+`order_media` snapshot ár; `markPaid()` — idempotens: token kiadás, kupon `used_count` növelése, `OrderConfirmationMail` queue-olása), `App\Services\StripeCheckoutGateway` (vékony wrapper a `stripe/stripe-php` SDK körül — **FONTOS**: a `StripeClient` **lusta** példányosítású (`client()` metódus, nem a konstruktorban), mert Laravel a route-metódus összes típusos függőségét (pl. `StripeCheckoutGateway $stripe`) a konténeren keresztül feloldja MIELŐTT a metódus törzse/validálása lefutna — ha a konstruktor azonnal `StripeClient`-et hozna létre, üres/hiányzó API-kulcs esetén ez már a validáció előtt 500-at dobna). `POST /checkout` (`Public\CheckoutController::store`) rendelést + Stripe session-t hoz létre; Stripe API-hiba esetén felhasználóbarát magyar hibaüzenetet ad vissza (nem a nyers SDK-kivételt). `GET /checkout/success` szinkron is ellenőrzi a session állapotát (nem csak a webhookra vár, hogy helyi teszteléskor is azonnal működjön), `POST /api/stripe/webhook` (a `routes/api.php`-ban, NEM web.php — stateless, nincs CSRF-probléma) az aszinkron/biztos forrás. Letöltés: `Public\DownloadController` — a token "5x használható" korlátja az OLDAL-megnyitásra vonatkozik (nem fájlonként), a fájlok a `local`/`nas` diskről (a `Media::original_storage` mező alapján) streamelve/ZIP-elve szolgálódnak ki. Mail driver fejlesztésben: `log` (a `storage/logs/laravel.log`-ba írja az e-maileket, nem küldi ki ténylegesen). Stripe teszt-kulcsok (`STRIPE_KEY`/`STRIPE_SECRET`/`STRIPE_WEBHOOK_SECRET`) még NINCSENEK kitöltve a `.env`-ben — a Stripe Dashboard "Test mode" API-kulcsaival kell pótolni, hogy a tényleges checkout-redirect működjön (addig felhasználóbarát hibaüzenetet ad a rendszer). 87 PHPUnit teszt zöld.

- **Fizetési szolgáltató absztrakció (Stripe + SimplePay)** kész — nincs új függőség (Laravel `Http` kliens + natív `hash_hmac`). `App\Services\Payments\`: `PaymentGateway` interfész (`provider()` / `isConfigured()` / `startPayment(Order): PaymentStartResult`), `PaymentGatewayManager` (`for($name)` / `default()` — config `payments.default`, ha nincs konfigurálva az első elérhető / `options()` a kosár választójához), `StripePaymentGateway` (a meglévő `StripeCheckoutGateway`-t csomagolja), `SimplePayGateway` (SimplePay **v2** — `POST {base}/payment/v2/start` HMAC-SHA384 aláírt JSON, `paymentUrl` redirect; minden aláírás `base64(hmac_sha384(nyers-body, secret_key))`). `PaymentException` → a `CheckoutController` felhasználóbarát 502-t ad. **SimplePay `orderRef` = `{PREFIX}-{order_id}-{rand}`** (a PREFIX a SiteBranding slug-ból; globálisan egyedi kell, retry új ref), a `payment_provider_reference` a `transactionId`-t tárolja (visszatérés/IPN elsődlegesen erre matchel, fallback az orderRef-be kódolt id). `SimplePayGateway` **NEM küld `invoice`-t** (csak e-mailt kérünk — a SimplePay a saját oldalán gyűjti; ha visszautasít, a `startPayment($order, $billing)` `$billing` paramba kell tenni + a kosárban bekérni). Visszatérés: `GET /checkout/simplepay/return` (`?r` base64 JSON + `?signature`, `e` = SUCCESS/FAIL/CANCEL/TIMEOUT) — **élmény, nem megbízható**. IPN (megbízható): `POST /api/simplepay/ipn` (`routes/api.php`, nincs CSRF) — `status: FINISHED` → `markPaid`, a válasz a kapott JSON + `receiveDate` + `Signature` fejléc (különben a SimplePay újrapróbál). `orders.payment_provider` enum már EPIC-02 óta `['stripe','simplepay','barion']`, nincs migráció. `CheckoutService::attachStripeSession` → `attachPaymentReference($order, $provider, $ref)`. `.env`: `PAYMENT_PROVIDER_DEFAULT` + `SIMPLEPAY_MERCHANT`/`SIMPLEPAY_SECRET_KEY`/`SIMPLEPAY_SANDBOX`. Kosár: `<radio>` fizetési mód választó, ha >1 szolgáltató konfigurált. 284 PHPUnit teszt zöld.

- EPIC-08 (superadmin dashboard + statisztikák) kész — `/admin/dashboard` (admin+superadmin): `App\Services\DashboardStatsService` számítja a 8 KPI kártyát (bevétel ma/hónap/összes, eladott média, aktív fizetett rendelés, aktív letöltési token, feltöltött média, hibás feldolgozás, fotósok aktív/inaktív, helyszín-feliratkozók = `EventSubscription` distinct e-mail count) + 5 Chart.js diagramot (bevételi trend 30 nap, kép/videó arány, top 5 esemény, top 5 fotós, feldolgozási státusz), plus legfrissebb események/rendelések panel (utóbbinál "Link újraküldés" gomb — `DashboardController::resendOrderEmail`) és biztonsági riasztás panel (`FailedLoginAttempt` modell + `LogFailedLoginAttempt` listener az `Illuminate\Auth\Events\Failed` eseményre — 24 órán belüli 5+ sikertelen bejelentkezés esetén pirosan jelez; az IP csak hashelve tárolódik). `/admin/stats`: `App\Services\SalesStatsQuery` közös szűrő-lekérdezés a lapozható táblázathoz ÉS a CSV exporthoz (`/admin/stats/export`), hogy a kettő sose térjen el egymástól. Chart.js diagramok színei a `resources/js/Composables/useChartColors.js`-en át a jelenlegi admin-beállított accent + világos/sötét téma CSS változóiból jönnek (reaktív a témaváltásra). **Fontos**: a `chart.js`/`vue-chartjs` importot (`resources/js/chartSetup.js`) NE az `app.js`-be tedd (eagerly bundle-olva minden nyilvános oldalra is bekerülne, +180 KB), hanem csak az `Admin/Dashboard.vue`-ba — így Vite külön chunk-ba code-split-eli, csak az admin dashboard látogatásakor töltődik be. 108 PHPUnit teszt zöld.
- Ismétlődő flaky teszt-minta, amire figyelni kell: `EventFactory`-nál a `name` mező alapértelmezetten `fake()->city().' Kanyar'` — ha egy teszt csak a `location`-t írja fölül explicit értékre (pl. 'Mátraháza') és a `name`-et nem, a hu_HU Faker city-generátor néha VALÓDI magyar városnevet ad vissza (pl. "Eger"), ami véletlenszerűen kollidálhat egy másik teszt keresési kulcsszavával (ILIKE `name` VAGY `location` mezőn). Mindig pineld le mindkét mezőt (`name` ÉS `location`) explicit, egyértelmű értékre bármely név/helyszín-alapú keresést tesztelő esetben.

- EPIC-09 (fotós menedzsment: CRUD + meghívó) kész — `/admin/photographers` (csak superadmin): lista keresés/szűrés (státusz/szerepkör) + "Új fotós meghívása" form + függőben lévő meghívók panel (Újraküldés/Visszavonás). Meghívási folyamat az EPIC-02-ben már megkezdett `invitations` táblára épül (`App\Models\Invitation`, token+48 órás lejárat) — **FONTOS**: ezt a táblát ki kellett egészíteni `name` és `revenue_share_percent` oszlopokkal (új migráció), mert az eredeti séma nem tartalmazta, pedig a meghívó űrlapnak már meghíváskor fel kell vennie ezeket, hogy elfogadáskor a `User` rekord készen legyen. **A `users` tábla `invitation_token`/`invitation_expires_at` oszlopai NEM használtak — feleslegesek/redundánsak** a különálló `invitations` táblával szemben (az EPIC-02 séma két versenyző meghívási mechanizmust vázolt fel, csak az `invitations` tábla lett kifejlesztve); ne ezekre építs, ha meghívó-logikát bővítesz. Publikus elfogadó oldal: `GET/POST /invitations/{token}` (`Auth\InvitationController`, guest middleware) — jelszó megadása után létrehozza a `User`-t (Spatie role automatikusan szinkronizálódik a meglévő `User::booted()` `saved` hook-on át, nincs kézi `assignRole()` szükséges), bejelentkezteti, `accepted_at`-et állít. Fotós részletes profil (`/admin/photographers/{user}`): Áttekintő tab (KPI-k, saját bevételi trend — `DashboardStatsService::revenueTrendForPhotographer()`, események, értékesítések, szerkeszthető profil+jutalék+aktív státusz, jelszó-visszaállítás `TemporaryPasswordMail`-lel, törlés csak média nélkül) + Tevékenység tab (Spatie ActivityLog `causer_id`-vel szűrve — ez már automatikusan működik minden meglévő `LogsActivity` modellen, mert a csomag alapból a bejelentkezett usert állítja causer-nek). 125 PHPUnit teszt zöld.

- EPIC-10 (helyszín-keresés: GPS + térkép) kész — a PostGIS `ST_DWithin`/`ST_Distance`-alapú GPS sugaras keresés backendje már az EPIC-06-ban elkészült (`App\Services\EventSearch`), de **soha nem volt tesztelve** (nulla teszt fedte a `distance_km`/sugaras keresést) és a frontend nem is mutatta/használta teljesen — ezt egészítettem ki: (1) `Welcome.vue` GPS fülén "Saját helyzet használata" gomb (`navigator.geolocation`, hibaüzenetek engedély-megtagadásra/időtúllépésre), (2) `EventMapModal.vue`-ban új "Keress itt" gomb — a látható térképrészlet közepéből + közép→ÉK-sarok távolságából (Leaflet `distanceTo()`) GPS sugaras keresést indít (1-50 km-re clamp-elve, ugyanaz a PostGIS-es AC mint a kézi GPS fülön), (3) `Events/Index.vue`: a találati kártyákon megjelenik a `event.distance_km` (csak GPS-szűrt keresésnél van jelen), plusz a `PublicLayout` "Helyszínek" navigáció (`/events?view=map`) végre tényleg megnyitja a térkép-modalt (`onMounted`-ban `?view=map` query param ellenőrzés) — korábban ez egy halott link volt. `tests/Feature/Public/EventGalleryTest.php`-ba 2 új teszt: sugaras szűrés közeli/távoli esemény helyes be/kizárása + `distance_km` mezo jelenléte, es tavolsag szerinti novekvő sorrend. 127 PHPUnit teszt zöld.

- Demo placeholder média — `App\Services\PlaceholderMediaGenerator` + `php artisan kanyarfotozas:generate-placeholder-media [--force]`: minden `Media` rekordhoz színes kártya-képet generál (esemény neve + sorszám + "KANYARFOTO · DEMO"), majd átengedi a VALÓDI `ImageProcessingService` pipeline-on (thumbnail / csempézett-vízjeles előnézet / letölthető JPEG+WebP), videóhoz FFmpeg-gel 8 mp-es lassan lebegő MP4-et + scrub sprite-ot. A `DemoDataSeeder` automatikusan meghívja a végén (a `migrate:fresh --seed` így képekkel teli demót ad, ~40 mp). NEM production kód. Megjegyzés: `migrate:fresh` nem törli a `storage/app/{public,private}` alatti régi fájlokat — újra-seedeléskor árvák maradhatnak, ártalmatlanok.
- Esemény-borítókép: `Event::scopeWithCoverThumbnail()` egy `cover_thumbnail_s3_key` pszeudo-oszlopot ad (az esemény első `ready` médiájának thumbnailja) — az `EventSearch` és az `Api\EventController::index` is használja, a `/events` lista + a főoldali "Friss fotózások" (ami már VALÓDI `/api/events` adatból jön, nem hardcode-olt mock) + a térkép-modal ezt jeleníti meg borítóként. **FONTOS**: a scope `addSelect`-et használ — a hívási helyeken `withCount()` UTÁN kell jönnie (különben az `withCount` `->select('events.*')`-ja kitörölné), és sose `->get(['oszlop', ...])` / `->select([...])` UTÁN (ugyanaz a select-felülírás csapda, mint az `EventFactory` flaky-nál).
- **`MediaFactory` javítás**: a `photo()`/`video()` state-ek korábban CSAK a `type` mezőt írták fölül, a típusfüggő kulcsokat (`preview_sprite_s3_key`, `download_jpeg_s3_key` stb.) nem — így egy `->video()` inkonzisztens rekordot adhatott (videó `preview_sprite_s3_key` nélkül). Most egy közös `keysForType()` helper adja mindkét state-nek az összes típusfüggő mezőt.
- 129 PHPUnit teszt zöld.
- Galéria lightbox (`resources/js/Components/MediaLightbox.vue`) — az `/events/{slug}` galériában egy médiára kattintva NEM navigál a `/media/{id}` oldalra, hanem egy teljes képernyős lightbox nyílik: bal/jobb léptetés (nyíl gombok + billentyűzet ←/→, Esc/X/háttérre kattintás zár), alul vízszintesen görgethető filmszalag (az aktív thumbnail accent kerettel, automatikusan középre görget), jobbra a média adatai (időpont/hossz/felbontás/fotós) + ár + Kosárba gomb. A galéria görgetési pozíciója megmarad (a lightbox tiszta kliens-oldali overlay, `position: fixed` body scroll-lock technikával — `overflow: hidden` nem elég, az nullázza a scrollt). A `MediaCard.vue` mostantól `@select` eseményt emittál (nem `<Link>`), a végénél a lightbox `@load-more`-t emittál a `Events/Show.vue` `loadMore()`-jához (infinite scroll a lightboxban lépkedve is folytatódik). A `/media/{id}` külön oldal (`Media/Show.vue`) MEGMARAD — a kosár + letöltő oldal linkjei oda mutatnak. Az `Api\EventController::media` és `Public\EventController::show` mostantól `->with('photographer:id,name')`-t is betölt (a lightbox sidebarhoz kell). 130 PHPUnit teszt zöld.

- EPIC-11 (információs aloldalak) kész — `/about` (`Info/About.vue`, `#photographers` horgony a nav "Fotósok" linkjéhez), `/privacy` (`Info/Privacy.vue` — GDPR + rendszám-homályosítás szabályai + `#cookies` horgony), `/shop` (`Info/Shop.vue` — árak, `base_price_huf` site_settingből, JPEG+WebP külön elmagyarázva), `/faq` (`Info/Faq.vue` — `App\Http\Controllers\Public\FaqController` a `FaqItem` táblából kategóriánként csoportosít, akkordeon + kategória-szűrő + schema.org **FAQPage** JSON-LD `<component :is="'script'" type="application/ld+json" v-html>`-lel a body-ban, mert Vue template nem enged `<script>` taget), `/contact` (`Info/Contact.vue` + `ContactController` — EGYSZERŰ űrlap (nincs típus-választó), védelem: **saját, külső szolgáltatás nélküli számtani captcha** ("Mennyi 7 + 3?" — a `create()` session-be teszi a választ, `store()` a `session()->pull()`-lal ellenőrzi + minden hibás beküldés után új kérdés) + **honeypot** `website` mező + `throttle:5,1` middleware; mentés `contact_messages` táblába (a `contact_type` oszlop marad, DB default 'support') + `ContactConfirmationMail` a feladónak + `ContactNotificationMail` az összes superadminnak `replyTo`-val). Statikus tartalom a Vue komponensekben (magyar jogi/infó szöveg), nem DB-ből. 137 PHPUnit teszt zöld.

- EPIC-12 (e-mail rendszer + riportok) kész — **Vásárlási visszaigazoló** (`OrderConfirmationMail` + `mail/order-confirmation.blade.php`): mostantól tételenként külön JPEG + WebP (fotó) / MP4 (videó) közvetlen letöltési gomb + "Összes ZIP-ben" gomb + letöltési oldal link (a direkt link nem "spend"-el token-használatot, mert az csak az oldal-megnyitásra számít). **Letöltési emlékeztető**: `SendDownloadReminders` parancs (`kanyarfotozas:send-download-reminders`) — `orders` új `reminder_sent_at` oszlop, a 48 órán belül lejáró (de nem lejárt/kimerült) fizetett rendelésekre egyszer küld `DownloadReminderMail`-t; **óránként** ütemezve. **Heti/havi fotós riport**: `SendWeeklyPhotographerReports` (`weeklyOn(1, '08:00')`) csak `report_weekly=true` aktív fotósoknak, `SendMonthlyPhotographerReports` (`monthlyOn(1, '08:00')`) `report_monthly=true`-nak **CSV melléklettel**; közös számítás `App\Services\PhotographerReport` (a `SalesStatsQuery`-t használja). Ütemezés a `routes/console.php`-ban `Schedule::command(...)`-mal — élesben `* * * * * php artisan schedule:run` cron kell. A fotós a saját `/photographer/dashboard`-ján kapcsolja be/ki a riportokat (`Photographer\ReportSettingsController`, `PUT /photographer/settings/reports`). Minden mail Laravel markdown-alapú → alapból mobil-responzív. 146 PHPUnit teszt zöld.

- E-mail sablon szerkesztő (superadmin, `/admin/settings/mail`) kész — `App\Services\MailTemplates` (regisztráció: 7 sablon — order_confirmation / download_reminder / contact_confirmation / contact_notification / photographer_invitation / temporary_password / photographer_report). Minden sablonnak 5 szerkeszthető szöveges mezője van (subject / heading / intro / outro / signature), a felülírások `site_settings`-ben JSON-ként `mail_tpl:{key}` kulcson, felülírás híján az alapértelmezettre esik vissza. `:token` alakú placeholder-ek (`:app_name`, `:order_id`, `:expires_at`, `:name`, stb.) — a `MailTemplates::resolve($key, $data)` behelyettesíti (hosszabb token előbb, hogy `:name` ne rontsa el `:app_name`-et). A mailable-ok az `App\Mail\Concerns\UsesMailTemplate` trait-en át (`templateKey()` + `templateData()`) kérik le a subject-et és a blade `with`-be szórt blokkokat; a blade nezetek `{{ $heading }}` / `{{ $intro }}` / `{{ $outro }}` / `{!! nl2br(e($signature)) !!}` — a dinamikus rész (gombok, tétellista, riport-táblázat) továbbra is kódolt. A Vue oldalon kliens-oldali élő előnézet minta-értékekkel. `POST /admin/settings/mail/{key}/reset` visszaállít alapértelmezettre.

- Fooldali hero diavetítés — kép ÉS videó (superadmin, `/admin/settings/hero`) kész — `hero_slides` tábla + `App\Models\HeroSlide` (`type` = image|video, `image_path` / `video_path` / `poster_path` a `public` diskon, `width`/`height`/`duration_seconds`, `sort_order`, `is_active`, `scopeActiveOrdered`, `isVideo()`). `Admin\HeroSlideController` (index/store/update/reorder/destroy): egy `file` mezőt fogad, a MIME alapján ágazik.
  - **Kép**: `ImageProcessingService::makeHeroImage()` max 2560px széles WebP 82%. Ajánlott **2560×1440 (16:9)**, max 12 MB (`IMAGE_MAX_KB`), min 1280×720.
  - **Videó**: `VideoProcessingService::makeHeroVideo()` — FFmpeg-gel max 1920px széles, **hang nélküli** (`-an`), `-crf 25 -maxrate 4M`, `+faststart` H.264 MP4 + egy WebP poszter-kocka (`extractFrame` + `makeHeroImage`). Szinkron fut a controllerben (nem queue-olt — rövid klip). Ajánlott **1920×1080 (Full HD), ~5 Mbps bitráta, ~15 mp, max 60 MB** (`VIDEO_*` konstansok) — mind kiírva az admin felületen 2 külön infó-dobozban.
  - A `/` route (closure a `web.php`-ban) `heroSlides` propként `[{type, url, poster}]` objektumokat ad át. `Welcome.vue`: kép → `<img>` Ken Burns zoommal, videó → `<video muted loop playsinline>` poszterrel; egymásba tűnnek (7 mp, 1.5 s opacity). Csak az aktív videó játszik (`syncHeroVideos` a `heroIndex` watcherből, `prefers-reduced-motion` esetén egy videó sem autoplay-el). Media hiányában a beépített sötét gradiens marad.
  - `PlaceholderMediaGenerator::generateHeroSlides()` a demóhoz 3 színes **kép** hero kártyát gyárt (a `DemoDataSeeder` + `kanyarfotozas:generate-placeholder-media` parancs hívja, üres `hero_slides` esetén).

- EPIC-13 (rendszámfelismerés + homályosítás) kész — **Plate Recognizer** (ingyenes szint: 2500/hó), adminból **teljesen kikapcsolható** (a fő kapcsoló OFF esetén EGYETLEN külső hívás sem történik).
  - **Beállítás**: `/admin/settings/plate-recognition` + `App\Services\PlateRecognitionSettings` (`site_settings`: `plate_recognition_enabled` / `_mode` = auto_blur|flag_only / `_min_confidence` 50–99 / `_provider` = platerecognizer|google_vision / `_api_key` **Crypt-titkosítva**). `isConfigured()` = enabled ÉS van kulcs. Üres api_key mező = "hagyd változatlanul", `clear_api_key` checkbox törli. A kulcs SOHA nem megy ki propként.
  - **Provider absztrakció**: `App\Services\PlateRecognition\` — `PlateRecognitionProvider` interfész, `PlateRecognizerProvider` (Http `Authorization: Token {key}`, multipart `upload`), `NullPlateRecognitionProvider` (kikapcsolt esetén). `PlateRecognitionManager::analyze($path): PlateAnalysis` — `skipped()` ha nincs konfigurálva; hiba esetén (API timeout) naplóz és `none`-ként tér vissza (a feldolgozás nem akadhat el). `analyzeFrames()` videóhoz (1/5/10 mp kocka, legjobb confidence).
  - **Pipeline**: `ProcessImageMedia` a variánsok előtt `analyze()`-t hív; `license_plate` / `_bbox` / `_confidence` / `_status` (pending|none|detected|unidentifiable) mentés. Ha `auto_blur` mód ÉS confidence >= küszöb → `ImageProcessingService::makeWatermarkedPreview/makeDownloadJpeg/makeDownloadWebp($path, $blurBox)` — a `blurRegion()` a box-ot a célváltozat méretére skálázza, kikódolt bájtokon át kimásolja a régiót (GD `clone` nem mély), `->blur(config('media.plate_blur_strength'))`, `->insert()` vissza. `license_plate_blurred = true`. **Videónál**: csak detektálás + jelzés (a rendszám mozog — nincs bake-elt blur), a médiaegészség panel jelez.
  - **Vásárlói hozzájárulás**: `orders.plate_consent` + a kosárban checkbox (`MediaResource.plate_blurred`), ha van homályosított tétel. `DownloadController::resolveKey($media, $format, $order)` — ha `plate_consent && license_plate_blurred` → a **pristine `original_s3_key`**-t szolgálja (fotónál) a homályosított download-változat helyett.
  - **Médiaegészség panel**: `pending` (feldolgozásra vár, ha enabled), `unidentifiable` (küszöb alatti), `video_review` (videó rendszámmal, nincs auto-blur).
  - `kanyarfotozas:analyze-plates {--limit=200}` — a funkció bekapcsolása UTÁN a korábban feldolgozott `pending` fotókra futtatja (auto_blur módban újragenerálja a homályosított változatokat).
  - `config/media.php`: `plate_blur_strength` (45), `plate_video_sample_seconds` ([1,5,10]).
  243 PHPUnit teszt zöld.

- EPIC-14 (HU/EN kétnyelvűség) kész (publikus oldalak) — **nincs vue-i18n függőség**: a fordítások `lang/hu.json` + `lang/en.json` flat kulcs→érték (pl. `"nav.home"`), Inertia shared prop-ként (`HandleInertiaRequests::share` → `locale` + `translations` closure), a `resources/js/Composables/useI18n.js` `t(key, replacements)` composable csinálja a lookup-ot + `:token\b` behelyettesítést. Nyelvváltás: `App\Http\Middleware\SetLocale` (session `app_locale`, a `web` middleware-ben ELSŐKÉNT), `PUT /locale/{locale}` (`LocaleController`), a `PublicLayout`-ban HU/EN kapcsoló (`router.put`, nem remountol → a fetch-elt listák subtitle-jét template-ben kell `t()`-zni, nem a fetch pillanatában stringgé sütni — ebbe belefutottam a `Welcome.vue` "friss fotózások" résznél). Lefordított publikus oldalak: PublicLayout, Welcome, Events (Index/Show), Media/Show, Cart, Checkout (Success/Cancel), Download/Show, Info (About/Shop/Contact/Faq/Privacy), MediaCard, MediaLightbox. **Admin/fotós felület szándékosan magyar marad** (Login, AcceptInvitation is). Backend locale-aware: `FaqController` (question_en/answer_en, HU fallback), `Country::name` accessor (`name_en` ?: `name_hu`, EN alatt — a public controllerek `name_hu,name_en` oszloppal töltik a relációt és `$country->name`-et használnak; az admin marad `name_hu`), `Info/Privacy.vue` külön HU/EN tartalom-struktúra a komponensben (v-html, statikus jogi szöveg). `app.blade.php` `<html lang>` már eleve `app()->getLocale()`. 168 PHPUnit teszt zöld.

- EPIC-18 (admin dashboard bővítések) kész — a `/admin/dashboard` (admin+superadmin) 5 új panellel bővült:
  1. **Proaktív figyelmeztetések** — `App\Services\ProactiveAlerts` (`all()` / `visible()` / `criticalVisible()` / `dismiss()`): a rendszerállapotból számított, cselekvést igénylő jelzések (hibás média = critical, elakadt feldolgozás / sprite hiány / lejárt announced esemény = warning, rég nem volt feltöltés / lejáró használatlan token / kimerült aktív kupon = info). Minden jelzésnek stabil `key`-e van → `dismissed_alerts` tábla (`alert_key` unique, `dismissed_by` **foreignUuid** — a `users.id` UUID!, `dismissed_until`), elrejtés `ProactiveAlerts::DISMISS_DAYS` (7) napra. `POST /admin/dashboard/alerts/dismiss`. A `kanyarfotozas:scan-alerts` parancs **15 percenként** (routes/console.php) e-mail-összefoglalót küld a superadminoknak, ha a kritikus jelzés-készlet változott ÉS nem ment ki az elmúlt 6 órában (állapot: `site_settings` `proactive_alerts_notified` JSON `{signature, at}`).
  2. **Konverziós tölcsér** — `DashboardStatsService::conversionFunnel()`: galéria megtekintés → rendelés elindítva → fizetés elindítva (`payment_provider_reference` not null) → kifizetve → letöltve (`download_token_uses > 0`). **NEM Plausible / nincs külső analitika**: a top-of-funnel a saját `event_views` napi rollupból jön (`Event::recordGalleryView()` a `Public\EventController::show`-ban, munkamenetenként egyszer, Postgres `ON CONFLICT ... DO UPDATE count+1`). A "galéria megtekintés" lépcső csak akkor jelenik meg, ha már gyűlt megtekintés az időszakban.
  3. **Médiaegészség** (`id="media-health"` — a proaktív jelzések ide linkelnek) — `DashboardStatsService::mediaHealth()`: failed / stuck (>2h processing) / videó sprite hiány / ready de hiányzó thumbnail-watermarked változat / rendszám-feldolgozásra vár (csak ha `PlateRecognitionSettings::enabled()`) + 10 példasor `Újrafeldolgozás` gombbal → `POST /admin/dashboard/media/{media}/reprocess` (status→processing + `ProcessImageMedia`/`ProcessVideoMedia` dispatch).
  4. **Bevétel-előrejelzés** — `DashboardStatsService::revenueForecast()`: az elmúlt 30 nap napi bevételére **least-squares lineáris regresszió**, extrapoláció a hét ill. hónap végéig (`forecast >= actual` garantált).
  5. **Fotós összehasonlítás** — `App\Services\PhotographerComparison` (`rows()` / `toCsv()`): fotósonként feltöltött/kész/eladott média, bevétel, `revenue_share_percent`-tel a részesedés, konverzió % (eladott/kész), átlagár. Kliens-oldali rendezés, `GET /admin/dashboard/photographers/export` CSV. A `PhotographerComparison` PostgreSQL `COUNT(*) FILTER (WHERE ...)`-t használ (a projekt Postgres-only).
  191 PHPUnit teszt zöld.
  - **Kiegészítés**: időszakos statisztika-export — `App\Services\PeriodicStatsExport` (`rollup(Carbon $from, Carbon $to, 'month'|'week')` → időszakonként egy sor: bevétel, kedvezmény, fizetett/létrehozott rendelés, fizetési konverzió %, átlag rendelés, egyedi vásárló, eladott média kép/videó bontásban, feltöltött média, aktív fotósok; `toCsv()`). `GET /admin/dashboard/stats/export?from=YYYY-MM&to=YYYY-MM&granularity=month` + a `Admin/Dashboard.vue` „Statisztika export" panelje (`<input type=month>` from/to + Havi/Heti). `COUNT(*) FILTER (WHERE ...)` (Postgres).

- EPIC-17 (vásárlói kényelmi funkciók) kész — 6 részfunkció:
  1. **Saját képeim** (`/my-purchases`) — OTP-alapú e-mail verifikáció (`App\Services\PurchaseLookup`, `purchase_otps` tábla: `email_hash`/`otp_hash` sha256, 10 perc TTL, max 3 kérés/óra). Verifikáció után a session `verified_purchase_email` kulcsba kerül; a `MyPurchasesController` listázza a kifizetett rendeléseket + letöltési linkeket, lejárt tokennél „Új letöltési link kérése". `PurchaseOtpMail`.
  2. **Kollekció / wishlist** — `resources/js/Stores/collection.js` Pinia store (localStorage `kanyarfotozas.collection`, a kosártól külön). Szív gomb a `MediaCard`-on, badge a `PublicLayout` navban, `/collection` oldal (`Public\CollectionController@index`, kliens hidratál `/api/cart?ids[]=`-ből). Megosztás: `POST /api/collection/share` → `collection_shares` tábla (rövid kód, 7 nap), `/collection/share/{token}` publikus. `POST /api/collection/track` fire-and-forget növeli a `media.wishlist_count`-ot (admin statisztikához).
  3. **Megosztás (vízjeles publikus link)** — `App\Services\MediaShareService`, `media_shares` tábla (uuid token, 30 nap). `POST /media/{media}/share` a letöltési oldalról ( érvényes `download_token`-nel bizonyítva a vásárlást), `/share/{token}` publikus oldal (`Public\SharedMedia.vue`, og:image meta, „Megvásárolom" CTA — MINDIG a vízjeles verzió). A Download oldalon tételenként megosztás gomb (Web Share API / vágólap).
  4. **Esemény-előhirdetés + feliratkozás** — `event_subscriptions` bővítve (`unsubscribe_token` uuid, `last_notified_at`). `POST /api/subscriptions` (email + location + opcionális country_code), az `Events/Show.vue` announced állapotában „Értesítést kérek" űrlap. `App\Observers\EventObserver` (`#[ObservedBy]` az Event modellen): ha a status `announced`/`draft` → `live`, dispatch `NotifyEventSubscribersJob` → helyszín-egyezés alapján `EventLiveNotificationMail` (100/perc rate limit, leiratkozó linkkel). `GET /unsubscribe/{token}`.
  5. **Hasonló időpontban javasló** — `GET /api/events/{event}/media/nearby?media_id=X&window_minutes=3&limit=6` (± perc ablak, kizárva a referenciát, időbeli közelség szerint rendezve — `EXTRACT(EPOCH FROM (shot_at - ?::timestamptz))`).
  6. **Fejlett időpont-navigáció** — `GET /api/events/{event}/media/histogram` (`[{hour, count}]`), `resources/js/Components/MediaHistogram.vue` mini bargraph a galéria tetején (kattintásra órára szűr), HH:MM kereső input az `Events/Show.vue` szűrősávban (±2 perc ablak a `shot_from`/`shot_to`-ra).
  **Query-param csapda**: `$request->validate(['x' => ['integer']])` a query stringből NEM cast-ol int-re → `(int)` kell, különben `Carbon::subMinutes("3")` TypeError (Carbon 3). Az `orders.download_token` UUID oszlop — tesztben is valós uuid kell.
  214 PHPUnit teszt zöld.

- EPIC-16 (videó HLS streaming) kész — új npm függőség (felhasználói jóváhagyással): **`hls.js`** (~575 KB, dinamikus `import()` → külön Vite chunk, sose az app.js-be).
  - `VideoProcessingService::makeHlsStream($src, $srcW, $srcH, $outputDir)` — a **vízjeles** előnézetből FFmpeg-gel minden `config('media.hls_variants')` szinthez (`720 => '2M'`, `480 => '1M'`) egy `{h}p.m3u8` + `{h}p_NNN.ts` szegmensek (`hls_time` = `config('media.hls_segment_seconds')` = 10 mp), plusz egy kézzel írt `master.m3u8` (`#EXT-X-STREAM-INF` BANDWIDTH+RESOLUTION). A forrásnál kisebb magasságú szint a forrásra clamp-el.
  - `media.hls_playlist_s3_key` új oszlop (a `master.m3u8` kulcsa a `public` diskon: `hls/{media_id}/master.m3u8`). `ProcessVideoMedia` a vízjeles MP4 után generálja + feltölti a teljes `hls/{id}/` könyvtárat; **hiba esetén `report()` + null** (a lejátszó visszaesik MP4-re). `MediaResource` exponálja.
  - `resources/js/Components/VideoPlayer.vue` — natív HLS (Safari/iOS) → közvetlen `.m3u8`; egyébként `hls.js` (dinamikus import); HLS hiba / nem támogatott → MP4 fallback (`src`). Használja: `MediaLightbox`, `Media/Show`, `Public/SharedMedia` (a `MediaCard` hover-előnézet marad MP4).
  - `PlaceholderMediaGenerator` (demó) is generál HLS-t a videókhoz (`--force`-szal újra). **Élesben a `public` disk URL-jét Cloudflare CDN mögé kell tenni** (a `.ts` szegmensek jól cache-elhetők) — ez deployment config, nincs kódban.
  216 PHPUnit teszt zöld.

- EPIC-15 (fotós mobil PWA) kész — új dev függőség (felhasználói jóváhagyással): **`vite-plugin-pwa`**.
  - **PWA setup**: `vite.config.js` `VitePWA` (`registerType: 'autoUpdate'`, `injectRegister: false`, `outDir: 'public/build'`, `modifyURLPrefix: { '': '/build/' }`, **NINCS `navigateFallback`** — az `createHandlerBoundToURL('/offline.html')` szinkron dobott, mert nem volt precache-elve, és ettől „ServiceWorker script evaluation failed"). A `sw.js` + `manifest.webmanifest` + `workbox-*.js` a `public/build/`-be kerül, egy saját Vite plugin (`copyPwaToPublicRoot`, `closeBundle order:'post'`) másolja a `public/` gyökérbe → a SW root scope-ban regisztrálható (`navigator.serviceWorker.register('/sw.js', { scope: '/' })`), a `./workbox-*` relatív import a gyökérben megtalálja a párját, a precache `/build/assets/...`-ra mutat. `app.blade.php`: manifest link + apple-touch/theme-color meta + regisztráló `<script>`. `clientsClaim: true` (deploy után az új SW azonnal átveszi a nyitott tabokat is). **Elavult chunk deploy után**: az `app.js` figyeli a `vite:preloadError` eseményt — ha egy kliens-oldali dinamikus import (Leaflet, lazy Inertia oldal) a már törölt régi hash-elt fájlt kérné, egyszer újratölti az oldalt (sessionStorage `kf.chunkReloaded` véd a ciklustól). Inertia saját asset-verzió ellenőrzése csak szerver-oldali navigációnál véd, a modál-nyitás nem az.
  - **Ikonok**: `php artisan kanyarfotozas:generate-pwa-icons` (`GeneratePwaIcons` — Intervention GD, akcent négyzet + „KF", `pwa-192/512`, `pwa-maskable-512`, `apple-touch-icon`).
  - **Feltöltő oldal** `/upload` (`MobileUploadController`, `role:superadmin|admin|photographer`) — mobil-optimalizált, `Pages/MobileUpload.vue`. Esemény-választó (fotós csak a sajátjait), admin fotós-választó, `<input type=file accept=image/*,video/* capture>`.
  - **`resources/js/Composables/useMobileUpload.js`**: fájlonkénti **XHR** feltöltés `upload.onprogress`-szal (a `POST /admin/events/{event}/media` végpontra), **IndexedDB offline sor** (`kf-uploads/queue`) — offline / hálózati hiba esetén a fájl (Blob) a sorba kerül, az `online` event + mount `drainQueue()`-t hív. Videó max 5 perc (kliens-oldali `readVideoDuration`). Fájlonkénti + összesített progress bar.
  233 PHPUnit teszt zöld.

- **Kritikus beállítások** (`/admin/settings/critical`, superadmin) kész — egy hub oldal a működéshez elengedhetetlen konfighoz:
  - **Állapot-áttekintő** (`App\Services\SystemReadiness::groups()`): Fizetés / Tárhely / E-mail / Várólista & ütemezés / Médiafeldolgozás / Alaprendszer — csoportonként OK/figyelmeztetés/hiba jelzés + link a saját beállítóoldalra. Ellenőrzi: van-e működő fizetési átjáró + webhook secret; `public/storage` symlink; NAS/R2 kulcsok; `MAIL_MAILER` (nem `log`/`array` élesben); `queue.default`; `failed_jobs`; **ütemező életjel** (`kanyarfotozas:heartbeat` 5 percenként írja a `site_settings.scheduler_last_run`-t → ha >20 perc, a cron valószínűleg nem fut); FFmpeg/FFprobe futtatható-e; `APP_KEY`/`APP_URL` https/`APP_DEBUG`; alapár.
  - **Fizetési kulcsok itt szerkeszthetők** (`App\Services\PaymentSettings` — a NasConnection mintájára `site_settings`-ben **Crypt-titkosítva**, `.env`/config fallback): Stripe secret / webhook secret / publishable, SimplePay merchant / secret key / sandbox, alapértelmezett szolgáltató. A `payment.settings` middleware (`App\Http\Middleware\ApplyPaymentSettings`) a checkout / success / simplepay-return / webhook / IPN / cart route-okon a `site_settings`-ből a config-ba tölti. Üres titkos mező = változatlan; `clear[]` checkbox törli.
  - **Az oldal neve / átnevezés** — `App\Services\SiteIdentity` a motor (`plan()` előnézet · `apply()` végrehajtás tranzakcióban · `scanCode()` / `scanDatabase()` az audithoz). Átírja a **nem vizuális** objektumokat: a PLATFORM saját domainjén levő e-maileket (`users` / `orders.buyer_email` / `contact_messages` / `event_subscriptions` / `data_requests` — a fotósok külső gmailje NEM), és a `site_settings` identitás-kulcsokat (`site_domain`, `mail_from_address`, `platform_name`, márkanév). A vizuális szövegeket (oldal-copy, FAQ) NEM — azok a saját szerkesztőoldalaikon.
    - **Admin**: `/admin/settings/critical` → „Az oldal neve / átnevezés" → **Előnézet** (`GET …/identity/preview`, JSON: e-mailek + beállítások + kód-találatok + `noop` flag ha nincs tényleges váltás) → **Az automatikus rész végrehajtása** (`POST …/identity/apply`). Az előnézet iskolás szintre lebontott, **számozott lépéssort** ad a kézi részhez (karbantartás mód → `ALTER DATABASE` → a `.env` 4 sora → `config:clear` → worker-restart → `up` → `audit-identity`), mindegyiknél HOL + MILYEN PARANCS.
    - **CLI**: `php artisan kanyarfotozas:apply-identity {domain} [--from=] [--dry-run]` · `php artisan kanyarfotozas:audit-identity [--token=kanyarfoto]` (a `scanCode`/`scanDatabase` az **új slug** előfordulásait kivágja, hogy a friss `kanyarfotozas:*` hivatkozások ne legyenek fals találatok — CI-barát, hibakóddal tér vissza).
    - **KÉZI marad** (a webből nem megy): a DB ÁTNEVEZÉSE (`ALTER DATABASE`) + `.env` (`DB_DATABASE` / `APP_NAME` / `APP_URL` / `MAIL_FROM_ADDRESS`) + `config:clear` + worker-újraindítás — az előnézet pontosan kiírja.
  - **A `kanyarfoto` → `kanyarfotozas` átnevezés MEGTÖRTÉNT**: a helyi DB (`kanyarfoto` → `kanyarfotozas`), az összes rendszer-e-mail, a `site_settings`, az **artisan parancsnévtér** (`kanyarfoto:*` → `kanyarfotozas:*`, minden `Schedule::command` + teszt), a NAS root / R2 bucket defaultok, a localStorage-kulcsok (`kanyarfotozas.theme` stb.), a tesztadatbázis (`kanyarfotozas_test`), az `about.intro` / `Privacy.vue` márkanevek. `php artisan kanyarfotozas:audit-identity` → **tiszta**.
  - A futásidejű, ügyfél felé látszó márkanevek már `SiteBranding::name()`/`slug()`-ból jönnek: `MailTemplates` default tárgyak (`:app_name` token), `PurchaseOtpMail`/`ProactiveAlertDigestMail` tárgy, `StripeCheckoutGateway` tétel-név, `DownloadController` ZIP/fájlnév, `Dashboard/StatsController` CSV export nevek, `DemoDataSeeder` rendszer-e-mailek (`SiteBranding::mailDomain()`, dev-ben `example.test`), `PlaceholderMediaGenerator` demó-szöveg.

- **Éles indulás blokkolók (folyamatban)** — a piros listából kész:
  1. **Cookie consent banner** — `resources/js/Stores/consent.js` (Pinia + localStorage `kanyarfotozas.cookie_consent`, `status` = null|essential|all, `analytics` getter előre bekötve jövőbeli analitikához) + `resources/js/Components/CookieConsent.vue` a `PublicLayout` alján. A footer „Cookie tájékoztató" gomb `consent.reset()`-tel újranyitja. Jelenleg csak működéshez szükséges cookie van (nincs analitika/marketing).
  2. **Rendeléskezelő + visszatérítés + nyomonkövetés** — `/admin/orders` (`Admin\OrderController`, admin+superadmin): lista, státusz-szűrő, részletek, letöltő e-mail újraküldése. **Sorszám**: `orders.order_number` = `{PREFIX}-{ÉV}-{6 jegy}` (PREFIX a `SiteBranding::slug()` első 3 betűje), `Order::booted()` `created` hookban az `id`-ból képződik (a migráció visszamenőleg is kitölti). Keresés e-mail / tranzakció **és sorszám** szerint. A sorszám megjelenik: admin lista/részlet, dashboard „legfrissebb rendelések", `Checkout/Success`, `Download/Show`, „Korábbi vásárlásaim", visszaigazoló + visszatérítés e-mail (`:order_number` token). **Eseménynapló** az admin részletnézetben: Spatie ActivityLog (`created`/`updated` + explicit `activity()->log('Fizetés beérkezett')` a `markPaid`-ben, `'Visszatérítés (teljes/részleges): X Ft — indok'` a `refund`-ban, `'Letöltő e-mail újraküldve'` a resendnél). **Megj.:** a projekt `activity_log`-ja csak leírást/subjectet/causert tárol, a `properties` (diff) üres — ezért az explicit `->log()` szöveges leírás, nem attribútum-diff. **Visszatérítés** (teljes/részleges): a `PaymentGateway` interfész bővült `refund(Order, int $amountCents): PaymentRefundResult`-tal — Stripe (`StripeCheckoutGateway::createRefund` a session → payment_intent alapján), SimplePay (`/payment/v2/refund` aláírt hívás, ezért a `payment_provider_order_ref` = orderRef is tárolódik a `PaymentStartResult`-ból). `CheckoutService::refund()` — `refunded_cents` akkumulál, teljes visszatérítésnél `payment_status = refunded` + a letöltési token azonnal lejár + kupon `used_count` vissza + `OrderRefundedMail` (új `order_refunded` mail sablon). `orders` új oszlopok: `refunded_cents`, `payment_provider_order_ref`, `refund_reference`, `refunded_at`, `refund_reason`.
  3. **GDPR adatkérelmek** — `data_requests` tábla + `DataRequest` modell. Publikus: `/adatvedelem/kerelem` (`Public\DataRequestController` — e-mail + típus [export|delete] + honeypot + `throttle:3,10`), e-mailes megerősítő link (`DataRequestVerifyMail`), `/adatvedelem/kerelem/{token}` verify. Admin (superadmin): `/admin/data-requests` (`Admin\DataRequestController`) — `PersonalDataExporter::forEmail()` (orders + contact_messages + event_subscriptions → JSON letöltés), `PersonalDataEraser::eraseEmail()` (orders `buyer_email` anonimizálás + token lejáratás [számviteli megőrzés miatt a rendelés marad], contact_messages/event_subscriptions/purchase_otps törlés). Footer + admin nav link.
  4. **Számlázás (Billingo v3)** — provider absztrakció a Payments mintájára: `App\Services\Invoicing\` (`InvoiceProvider` interfész, `BillingoInvoiceProvider`, `NullInvoiceProvider`, `InvoiceManager`, `IssuedInvoice`, `InvoiceException`), `App\Models\Invoice` (`invoices` tábla: provider/type[normal|storno]/storno_of/external_id/number/gross_cents/pdf_path/status[issued|failed]/error/attempts/issued_at). Beállítás: `/admin/settings/critical` „Számlázás" szekció + `App\Services\InvoiceSettings` (`site_settings`: `invoice_provider` / `invoice_api_key` **Crypt-titkosítva** / `invoice_block_id` / `invoice_vat` / `invoice_auto`; `PUT /admin/settings/critical/invoicing`). `isConfigured()` = provider≠none ÉS van kulcs ÉS (billingo → van blockId). **Egy összegző sor** számlánként (bruttó `total_cents`), így a kupon-kedvezmény sose okoz kerekítési eltérést. **ÁFA**: alap `AAM` (alanyi adómentes), EU-n kívüli vevő országnál automatikus `EU-N KÍVÜLI` (`BillingoInvoiceProvider::vatFor()` 27-országos EU-lista), OSS/külföldi ÁFA-t könyvelővel kell beállítani (az admin szekció alján van egy részletes ÁFA-magyarázó doboz). **Auto-számlázás**: `invoice_auto` ON esetén `CheckoutService::markPaid()` után `IssueInvoiceJob` (queue, `tries=3`, exponenciális backoff); teljes visszatérítéskor `CheckoutService::refund()` → `StornoInvoiceJob` (Billingo `/documents/{id}/cancel`). Idempotens: `issueFor()` a meglévő kiállított normál számlát adja vissza. A PDF a `local` (STAGING) diskre kerül (`invoices/{order_id}-...pdf`). **Számla-checkout**: ha `InvoiceManager::isConfigured()`, a pénztár (`Cart/Index.vue` `<fieldset v-if="billingRequired">`) bekéri a vevő nevét + országát (kötelező), opcionálisan irsz/város/cím + „Céges számlát kérek" → adószám; `CheckoutController::store` `Rule::requiredIf($needsBilling)`. `orders` új oszlopok: `billing_name/country/zip/city/address/tax_number`. **Számla-elérés**: admin rendelés-részlet „Számla" panel (kiállítás / sztornó gomb + PDF-link — `POST /admin/orders/{order}/invoice`, `POST /admin/invoices/{invoice}/storno`, `GET /admin/invoices/{invoice}/pdf`); vevőnek a letöltő oldalon + a visszaigazoló e-mailben „Számla (PDF)" link (`GET /download/{token}/invoice`). `SystemReadiness::payments()` jelzi, ha nincs számlázó (magyar webshopnál kötelező). `Http::fake()`-elt tesztek (`tests/Feature/InvoicingTest.php`, 6 eset).
  5. **Fizetési módok ki/be kapcsolása** — `PaymentSettings::providerEnabled('stripe'|'simplepay')` (`site_settings` `payment_enabled:{provider}`, hiány = engedélyezve). `PaymentGatewayManager::available()` = konfigurált ÉS engedélyezett; `default()` / `options()` / `CheckoutController::store` mind az `available()`-t nézi. Admin: a „Kritikus beállítások" fizetés-szekcióban providerenként „Elérhető a pénztárban" checkbox.
  6. **Fotós kifizetés-elszámolás** — `photographer_earnings` (egy sor / megvásárolt `order_media` tétel, amelynek van fotósa: `gross_cents` + `share_percent` **snapshot** a `markPaid` pillanatában, `amount_cents = round(gross*share/100)`, `status` = pending|paid|reversed, `payout_id` nullable) + `photographer_payouts` (`payout_number` = `{PREFIX}-KIF-{ÉV}-{4jegy}` az `Order`-hez hasonló `created` hookkal — az oszlop **nullable**; `status` = draft|paid, `method`/`reference`/`note`/`paid_at`/`created_by`). `App\Services\PhotographerPayoutService`: `recordForOrder(Order)` (idempotens, `CheckoutService::markPaid` hívja), `reverseForOrder(Order)` (teljes visszatérítéskor a **pending** tételeket `reversed`-re állítja — a `CheckoutService::refund` `$isFull` ágán), `outstanding(User)` / `summary()` / `globalOutstanding()`, `createDraft(User, ?until, meta)` (a nyitott tételeket egy draft bizonylatba vonja, `lockForUpdate`), `markPaid(Payout, meta)` (a bizonylat + a tételei `paid`), `deleteDraft(Payout)` (a tételek visszakerülnek a nyitott egyenlegbe), `ledger(User)` / `ledgerCsv(User)`. Admin (superadmin): **a felület a Fotósok admin lapon van** (`/admin/photographers`) — a lista alatt „Kifizetetlen jutalék összesen" + „Eddig kifizetve" számláló (`PhotographerController::index` a `PhotographerPayoutService::summary()`-ból), a listában „Nyitott jutalék" oszlop fotósonként; a fotós nevére kattintva a `/admin/photographers/{user}` részletnézet **„Kifizetések" füle** (csak fotós szerepkörnél, `PhotographerController::show` a `panelFor($user)` propból) — KPI-k, bizonylat-előkészítés/kifizetés/visszavonás, főkönyv-tábla + `GET /admin/photographers/{user}/payout/export` CSV. Műveletek: `Admin\PayoutController` (`store`/`markPaid`/`destroy`/`export`, nincs `index`/`show`), route-ok: `POST /admin/photographers/{user}/payout`, `POST /admin/payouts/{payout}/paid`, `DELETE /admin/payouts/{payout}`. A fotós a saját `/photographer/dashboard`-ján látja a nyitott + kifizetett összegét és a bizonylatait (read-only). **A `create_photographer_earnings` migráció visszamenőleg feltölti** a már kifizetett rendelések jutalékait (`INSERT … SELECT`, `share_percent` a fotós aktuális `revenue_share_percent`-jéből). (Korábban külön `/admin/payouts` oldal + „Fotós elszámolás" readiness-csoport + AdminLayout „Kifizetések" nav volt — mind megszűnt, a `globalOutstanding()` a service-ben már nem használt.)
  **A piros lista (kötelező éles-indulás blokkolók) ezzel kész.**
  314 PHPUnit teszt zöld.

- **SEO alapok** kész — `App\Services\Seo::forRequest()` az EGYETLEN forrás a publikus meta-adatokhoz (title / description / canonical / OG / Twitter / robots / JSON-LD). Két helyen fogy: (1) `View::composer('app', …)` a blade `$seo` változóhoz → **szerver-oldali** meta az első lefestéshez ÉS a JS-t nem futtató közösségi link-preview botokhoz (`app.blade.php` `@if (! empty($seo))` blokk); (2) `HandleInertiaRequests` `seo` shared prop → a `resources/js/Components/SeoHead.vue` (a `PublicLayout`-ban) `head-key`-elt `<Head>` tagekkel tartja szinkronban SPA-navigációnál (a `<title>`-t szándékosan NEM ő kezeli — az marad az oldalankénti `<Head :title>` lokalizált `t()` értéke). **Canonical mindig query string nélkül** (`$request->getPathInfo()`), hogy a szűrt/lapozott listák ne osszák meg a rangsort. Route-specifikus címek/leírások: esemény (`public.events.show` — a `withCoverThumbnail()` borítókép mint OG-kép + schema.org **Event** JSON-LD; **figyelem**: a scope `addSelect`-es, ezért `->first()?->cover_thumbnail_s3_key`, nem `->value()`), média, megosztás (`share_token`, nem `token`), és a statikus infó-oldalak. **Noindex** (`Seo::NOINDEX_ROUTES`): kosár / pénztár / letöltés / saját képeim / kollekció / megosztás / login / meghívó / GDPR-kérelem / feltöltő. Főoldalon `WebSite` + `Organization` JSON-LD. **Dinamikus `/sitemap.xml`** (`App\Http\Controllers\SitemapController`, 1 órás cache) — a statikus infó-oldalak + minden `live` esemény (`/events/{slug}`, `lastmod`); a média-részletoldalak szándékosan kimaradnak. **Dinamikus `/robots.txt`** (ugyanaz a controller) — a privát területeket tiltja + `Sitemap:` sor; a régi statikus `public/robots.txt` **törölve** (a route csak akkor él, ha nincs statikus fájl). A FAQ oldal a saját FAQPage JSON-LD-jét tartja meg. **Admin: `/admin/settings/seo`** (superadmin, „SEO" menüpont) — `App\Services\SeoSettings` + `Admin\SeoSettingsController` (`site_settings`): alap meta-leírás (`seo_description`), cím-kiegészítés (`seo_title_suffix`), OG-kép feltöltés (`seo_og_image_path` a `public` diskon, `ImageProcessingService::makeHeroImage`-dzsel WebP-re alakítva; hiány → a bundle-olt minta), Google Search Console azonosító (`seo_google_verification` → `<meta name="google-site-verification">`), és a **globális „kereshetőség" kapcsoló** (`seo_search_visible`, alap: be) — KI esetén MINDEN oldal `noindex, nofollow` és a `/robots.txt` `Disallow: /` (indulás előtti / karbantartási mód). Az `App\Services\Seo` és a `SitemapController::robots` innen olvas.
  324 PHPUnit teszt zöld.

- **GEO (AI-kereshetőség)** kész — a klasszikus SEO-réteg (fent) adja a GEO nagy részét; ez EGY plusz: a dinamikus **`GET /llms.txt`** (`App\Http\Controllers\LlmsController`, 1 órás cache, `Markdown` az llms.txt konvenció szerint) — szerkeszthető oldal-leírás + a fő oldalak + a `FaqItem` kérdés-válaszok + a `live` események listája. `App\Services\GeoSettings` (`site_settings`: `llms_txt_enabled` alap be, `llms_txt_description`). **Admin: `/admin/settings/geo`** (superadmin, „GEO (AI-keresők)" menüpont, `Admin\GeoController` + `Admin/Settings/Geo.vue`) — a Vue oldalon egy **hosszú EMLÉKEZTETŐ blokk** magyarázza a működést + a leírás szerkeszthető + toggle + előnézet-link. `LlmsController` tiszteletben tartja a `SeoSettings::searchVisible()`-t (indulás előtti módban `/llms.txt` → 404). A `FaqItemSeeder` bővült vásárló/fotós/szervező-szándékú kérdésekkel. **Nincs referral-tracking és nincs „GEO funkció" a vásárlói tölcsérben — tudatosan** (tranzakciós, hiperlokális termék). Teszt: `tests/Feature/GeoLlmsTxtTest.php` (5).

- **Hibakövetés + automatikus mentés** kész — külső szolgáltatás nélkül.
  - **Hibakövetés**: `bootstrap/app.php` `$exceptions->report()` → `App\Services\ErrorReporter` (sose dob — minden külső hívás `rescue(..., null, false)`-ban). A kezeletlen kivételeket ujjlenyomat szerint (`sha256(class|file|line)`) csoportosítja az `error_events` táblába (`App\Models\ErrorEvent`: count, first/last_seen_at, resolved_at, url, method). A 4xx / `ValidationException` / `AuthenticationException` / `AuthorizationException` / `TokenMismatchException` / `ThrottleRequestsException` / `ModelNotFoundException` kihagyva. Lezárt hiba újbóli előfordulásra automatikusan újranyílik. **Értesítés** (fingerprint-enként cache-cooldown `monitoring.error_cooldown_minutes`, alap 30 perc): `ErrorNotificationMail` a superadminoknak (`MonitoringSettings::notifyEmail()`, kikapcsolható) + opcionális `Http::post` egy Slack/Discord-kompatibilis webhookra (`error_webhook_url`, **Crypt-titkosítva** a `site_settings`-ben). Admin: `/admin/errors` (`Admin\ErrorEventController`, superadmin, „Hibanapló" menüpont) — lista/szűrő (nyitott/lezárt/összes) + keresés, „Lezár"/„Újranyit"/„Töröl"/„Összes lezárása".
  - **Automatikus mentés**: `App\Services\BackupService::run()` — `pg_dump` (`monitoring.pg_dump_binary`) → `gzencode` → `Storage::disk(monitoring.backup_disk)` (`backups/db-{timestamp}.sql.gz`), majd a `monitoring.backup_keep`-nél (alap 14) régebbieket törli. **Csak a DB** kerül mentésre (a média a NAS/R2 archív rétegen él saját redundanciával). `kanyarfotozas:backup` parancs (`routes/console.php`: `dailyAt('03:15')`) — hiba esetén `MonitoringSettings::recordBackup(false, ...)` + `ProactiveAlertDigestMail` a superadminoknak. Admin: a Kritikus beállítások „Monitoring és mentés" szekciója — webhook + e-mail kapcsoló, „Mentés most" gomb (`POST /admin/settings/critical/backup`), a legutóbbi 10 mentés letöltő linkkel (`GET /admin/settings/critical/backup/{name}`, path-traversal-védett). `SystemReadiness` új „Monitoring & mentés" csoport: 24 órán belüli nyitott hibák száma + az utolsó mentés kora/állapota.
  - **Megj. (Windows dev)**: a „Mentés most" GOMB a `php artisan serve` alatt elhasal (`sf_proc_00.out.lock` — Symfony Process nem tud a rendszer-temp-be írni a web-process alól, ugyanaz a jelenség mint az FFmpeg-ellenőrzésnél). Terminálból (`php artisan kanyarfotozas:backup`) és éles Linux-szerveren működik — a napi cron is CLI-ből fut.
  - `.env.example`: `ERROR_COOLDOWN_MINUTES` + `PG_DUMP_BINARY` / `BACKUP_DISK` / `BACKUP_PATH` / `BACKUP_KEEP`. `config/monitoring.php`.
  331 PHPUnit teszt zöld.

- **Bejelentkezés-védelem (brute-force)** kész — `App\Http\Requests\Auth\LoginRequest` (a `AuthenticatedSessionController::store` ezt kapja), három réteg:
  1. **E-mail + IP** páronként 5 sikertelen próba után 15 perces cache-alapú `RateLimiter` időzár (`MAX_PER_IDENTITY`).
  2. **IP-nként** 25 sikertelen próba / 15 perc (`MAX_PER_IP`) — e-mail-cím-forgatás ellen.
  3. **Fiók-szintű zár**: egy e-mail-cím 10 sikertelen próba / 15 perc után **bármely IP-ről** zárolódik (a `failed_login_attempts` naplóból számolva — elosztott támadás ellen), `ACCOUNT_LOCK_THRESHOLD` / `_MINUTES`. Sikeres belépés törli a limitert ÉS az adott e-mail `failed_login_attempts` sorait.
  Rate-limitnél `Illuminate\Auth\Events\Lockout` esemény. **Felhasználó-enumeráció ellen**: ismeretlen e-mail és rossz jelszó egyaránt „Hibás e-mail cím vagy jelszó." — csak helyes jelszóval, inaktív fióknál kap „A fiók inaktív" üzenetet (ekkor azonnali `Auth::logout()`). A meglévő `FailedLoginAttempt` + `LogFailedLoginAttempt` (auto-discovered listener a `Failed` eseményre) + a dashboard biztonsági panel változatlan. Teszt-környezet: `CACHE_STORE=array` → a RateLimiter állapota tesztenként nullázódik (a `refreshApplication` friss konténert ad). 342 PHPUnit teszt zöld.

- **Kétfaktoros hitelesítés (2FA / TOTP)** kész — új Composer-függőségek (jóváhagyva): `pragmarx/google2fa` + `bacon/bacon-qr-code` (ugyanaz, amit a Laravel Fortify használ).
  - `App\Services\TwoFactor`: `generateSecret()` / `qrCodeSvg()` (BaconQrCode SVG) / `verify()` (`window: 1`) / `enable()` (függőben lévő titok + 8 helyreállító kód, **Crypt-titkosítva** a `users.two_factor_secret` / `_recovery_codes` oszlopokban) / `confirm()` (kód → `two_factor_confirmed_at`) / `disable()` / `consumeRecoveryCode()` / `regenerateRecoveryCodes()`. Globális szabály: `two_factor_required` (`site_settings`).
  - **Beállítás**: `/admin/settings/security` (`Admin\SecuritySettingsController`, **minden admin a sajátját**, „Biztonság" menüpont) — bekapcsolás → QR + kézi kulcs + helyreállító kódok → kód megerősítése; kikapcsolás **jelszóval**; superadmin itt teheti kötelezővé mindenkinek (előbb a sajátján be kell kapcsolnia).
  - **Belépési folyamat**: `LoginRequest::authenticate()` a jelszó után — ha `hasTwoFactorEnabled()` ÉS nem megbízható eszköz → `login.2fa` munkamenet-kulcs + a controller a `/two-factor-challenge`-re irányít (NEM jelentkezteti be). `Auth\TwoFactorChallengeController` (`guest` group, `throttle:10,1`): 6 jegyű TOTP VAGY helyreállító kód (egyszer használható), 5 perces munkamenet-TTL, IP+user szintű 5/15-perc rate-limit. „Megbízom ebben a gépben (30 nap)" → **állapotmentes, saját Crypt-titkosított + hitelesített süti** (`kf_2fa_device`, a `EncryptCookies` `except` listáján — nincs dupla titkosítás; a titok bármilyen változása érvényteleníti).
  - **Kikényszerítés**: `2fa` middleware (`EnsureTwoFactorIsSetUp`) a 3 admin route-csoporton — ha `two_factor_required` be van kapcsolva és az adminnak nincs 2FA-ja, a belépés után csak a `/admin/settings/security` + `logout` érhető el.
  - `users` új oszlopok: `two_factor_secret` / `two_factor_recovery_codes` / `two_factor_confirmed_at` (mind `#[Hidden]`). Tesztek: `tests/Feature/TwoFactorTest.php` (11 eset).
  353 PHPUnit teszt zöld.

- Site branding / oldal neve (`/admin/settings/branding`, superadmin) kész — `App\Services\SiteBranding` (`site_settings`: `site_name` / `site_logo_lead` / `site_logo_tail`). **A név mentése átírja a `watermark_text`-et is** (`mb_strtoupper($name)`) — a `WatermarkSettings::text()` alapból szintén `SiteBranding::watermarkText()`. A kétszínű fejléc logó egy közös `resources/js/Components/BrandLogo.vue` (Inertia shared `branding` prop) — PublicLayout / AdminLayout / Login / AcceptInvitation / MobileUpload mind ezt használja (a hardcode-olt `KANYAR<span>FOTÓS</span>` helyett). Az oldalcím (`app.blade.php` `<title inertia>`) és a mail aláírások (`{{ $brandName }}`) egy `View::composer(['app', 'mail.*'], ...)`-ból jönnek (render-időben olvas, nem boot-kor — így a teszt is látja a friss értéket). `app.js` `brandName` a shared prop `branding.name`-ből. `HandleInertiaRequests` shared: `appName` + `branding`. `MailTemplates` `:app_name` szintén `SiteBranding::name()`.

- **Üzenetkezelő (kapcsolati szálak + válaszok)** kész — a `contact_messages` szálasítva: `photographer_id` (nullable, `users` FK) + `event_id` + `last_reply_at` oszlop, új `contact_replies` tábla (`contact_message_id`, `author_id` = a válaszoló admin/fotós, `on_behalf_of_id` = ha az admin egy fotós NEVÉBEN válaszolt, `body`, `emailed`). `App\Models\ContactMessage` bővült (`replies()` / `photographer()` / `event()` reláció, `TYPE_*` + `STATUS_*` konstansok, `last_reply_at` cast), `App\Models\ContactReply` (`author()` / `onBehalfOf()`). Közös logika: `App\Services\ContactThread::reply($message, $body, $author, ?$onBehalfOf)` — rögzíti a választ, `last_reply_at` + `status=in_progress`, kiküldi a `ContactReplyMail`-t a feladónak, activity-log. **Admin** (`/admin/messages`, admin+superadmin, „Üzenetek" nav): `Admin\MessageController` (`index` szűrőkkel: összes / megválaszolatlan / fotósnak címzett / support / lezárt; `reply` — `on_behalf_of_id` csak a szálhoz rendelt fotós lehet, különben 422; `updateStatus`). Kétpaneles inbox (`Pages/Admin/Messages/Index.vue`), a fotósnak címzett szálnál „Válasz X fotós nevében" checkbox. **Fotós** (`/photographer/messages`, „Üzenetek" nav): `Photographer\MessageController` — CSAK a `photographer_id = auth()->id` szálak, válasz (nem a saját szálára → 404). **Publikus belépési pont**: a `/media/{media}` oldalon „Kérdés a fotóshoz" űrlap (csak ha van fotós) → `POST /media/{media}/question` (`Public\PhotographerQuestionController`, `throttle:5,1`), `contact_type=photographer` + `photographer_id`/`event_id` kitöltve, e-mail a fotósnak + superadminoknak. A captcha közös: `App\Support\ArithmeticCaptcha` (`issue()` / `check()` nem pull-öl, `forget()` sikeres beküldés után) — a `ContactController` is erre állt át (session kulcs változatlan: `contact_captcha_answer`). Új mail sablon: `contact_reply` (a `/admin/settings/mail` felületen szerkeszthető, `:replier` token = a fotós neve vagy a csapat). Tesztek: `tests/Feature/MessageManagerTest.php` (9 eset).
  366 PHPUnit teszt zöld.

- **Kézi kép-import FTP-ről / NAS-ról** kész — „Beolvasás FTP-ről" panel **az új-esemény űrlapon** (`Admin/Events/Form.vue`, create) ÉS az esemény szerkesztő+média oldalán (`Admin/Events/Show.vue`) — közös `resources/js/Components/FtpImportBrowser.vue` komponens (fájlböngésző: breadcrumb, mappanavigáció, checkbox + „Mind kijelöl"; `v-model:paths` + `v-model:photographerId`). Csak admin/superadmin; a fotósok a sima webes feltöltést használják, a megosztott fájlszervert nem böngészhetik.
  - `App\Services\FtpImport` (a `nas` SFTP diskre épül — a felhasználói szóhasználatban „FTP szerver"): `isAvailable()` = `NasConnection::isConfigured()`, `browse($path)` → almappák + képfájlok (jpg/jpeg/png/webp) breadcrumb-bal, `..` traversal tiltva (`InvalidArgumentException`), `import(Event, $paths, $photographerId)` → minden kiválasztott távoli fájlt `readStream` → lokális staging → `MediaIngestor::ingestStaged()` (tartalom-hash dedup + esemény-ár + `Media` + `ProcessImageMedia::dispatch`). Kétszeres importálást kiszűr: azonos forrás-útvonal (gyors elő-szűrés, `import_source_path`) VAGY azonos fájltartalom (`content_hash`) egy eseményben csak egyszer.
  - **Böngésző-endpoint eseménytől független**: `GET /admin/media-import/browse` (`Admin\MediaImportController::browse`, controller-szinten `abort_unless($user->isAdmin())`) — az új-esemény űrlap és a részletnézet is ezt hívja.
  - **Import a részletnézetből**: `POST /admin/events/{event}/import` (`MediaImportController::store`).
  - **Import létrehozáskor**: a `StoreEventRequest` opcionális `import_paths[]` + `import_photographer_id` (`required_with:import_paths`), az `EventController::store` az esemény létrehozása után lefuttatja az importot (`Arr::except` a `Event::create`-ből), a flash üzenet összegzi.
  - Új oszlop: `media.import_source_path` (nullable). `EventController::show` és `::create` `ftpImport` propot ad (`available` bool, csak adminnak) + a `create` a `photographers` listát is.
  - Tesztek: `tests/Feature/MediaFtpImportTest.php` (8 eset), `tests/Feature/MediaDedupAndPricingTest.php` (7 eset — duplikátum-szűrés web+FTP, esemény-ár öröklés/átárazás, `MediaController::update` már nem árat állít).

- **Események admin lista szűrése** (`/admin/events`, `EventController::index`) — a puszta név-keresés helyett: **szabad szöveg** (esemény neve VAGY helyszín VAGY ország neve, `ILIKE` — a Postgres ILIKE ékezet-érzékeny, nincs `unaccent`), **ország** select, **fotós** select (`whereHas('media', photographer_id)` — csak adminnak), **dátum-tartomány** (`event_date` `whereDate` `>=` / `<=`). A `filters` prop + `countries` + `photographers` (utóbbi csak adminnak). `Admin/Events/Index.vue` szűrősáv „Szűrés" + „Törlés" gombbal, a query string `withQueryString()`-gel megmarad a lapozáson.
  388 PHPUnit teszt zöld.

- **Forensic (láthatatlan) vízjel a letöltésekbe** kész — `App\Services\ForensicWatermark`. A jel a **letöltés pillanatában, memóriában** kerül a fájl bájtjaiba (a tárolt fájl NEM változik, nincs FTP-oda-vissza): JPEG-nél egy COM (comment) szegmens az SOI után + minden formátumnál egy záró marker; a payload `v1|order_id|media_id|timestamp` + HMAC-aláírás (`config('app.key')`, 20 hex). Kép-újrakódolás NINCS → gyors, nem rontja a minőséget. `DownloadController::file` + `::zip` a fotó JPEG/WebP letöltéseket stampeli (a plate-consent pristine eredetit is), `download_fingerprints` naplósor (order/media/format/ip_hash). **Admin**: `/admin/forensics` (superadmin, „Kép-visszakövetés" nav) — feltöltöd a gyanús képet → `ForensicWatermark::read()` visszafejti (HMAC-ellenőrzött) → rendelés sorszám + vásárló e-mail + esemény + letöltés-napló. **Ki/be kapcsolható**: `site_settings.forensic_watermark_enabled` (alap: be), toggle a forensics oldalon. **KORLÁT** (dokumentálva a UI-n): szándékos metaadat-eltávolítás (teljes újratömörítés, `exiftool -all=`) törli — elrettentés + gondatlan továbbadás visszakövetése, NEM feltörhetetlen DRM (ahhoz pixel-szintű DSP-vízjel kellene). Teszt: `tests/Feature/ForensicWatermarkTest.php` (6).

- **Esemény-szervező (organizer) portál** kész — új `users.role` érték: `organizer` (enum + `invitations.role` enum bővítve migrációval; Spatie `Role` a seederben). `events.organizer_id` (nullable FK) + `events.organizer_share_percent`. `User::isOrganizer()` / `organizedEvents()`, `Event::organizer()`. `App\Services\OrganizerRevenue`: `forEvent()` (bruttó fizetett eladás az esemény médiáiból, rendelés/vásárló szám, `share_cents = round(gross * percent/100)`, `paid_cents` az `organizer_payouts`-ból, `outstanding`), `summary(User)` / `totals(User)`. **Portál** (`role:organizer`, `/organizer/dashboard` + `/organizer/events/{event}`, READ-ONLY): a szervező a saját eseményeinek statisztikáját + a részesedését látja, CSV export. **Admin**: az esemény-űrlapon (create + Show.vue szerkesztő) „Szervező" select + „részesedés %" (`required_with:organizer_id`, csak admin állíthatja — `Arr::except` nem-adminnál); `/admin/organizer-payouts` (superadmin, „Szervező kifizetések" nav) — szervezőnként felhalmozott / kifizetve / nyitva + egyszerű kézi kifizetés-rögzítés (`organizer_payouts` tábla: `organizer_id`, `event_id?`, `amount_cents`, `reference`, `note`, `paid_at`). A meghívó (`/admin/photographers`) is felveszi az `organizer` szerepkört. Bejelentkezés után az organizer a `/organizer/dashboard`-ra kerül. AdminLayout `organizerNav` + „Szervező felület" fejléc. Tesztek: `tests/Feature/OrganizerPortalTest.php` (8).
  409 PHPUnit teszt zöld.

- **Közös kereső-panel** — a főoldali komplex kereső (Ország / Helyszín-autocomplete / Időpont / Fotós / Típus + GPS-fül + Térkép-modal) kiemelve `resources/js/Components/EventSearchPanel.vue`-ba, és **MINDKÉT helyen** megjelenik: `Welcome.vue` (a hero alsó szélére ülve) ÉS `Events/Index.vue` (a lista tetején, a régi egyszerű kereső-sor helyett). A panel `initial` propból előtölt (a `/events` `filters`), submitre `router.get('/events', params)`. `Events/Index.vue`-ból kikerült a saját map-modal state + az egysoros kereső.
- **Publikus „Fotósok" oldal** (`/photographers`, `Public\PhotographerController` → `Info/Photographers.vue`) — a nyilvános (`users.is_public` + `is_active`) fotósok ÉS adminok: kör alakú profilkép (vagy monogram-placeholder), mellette a név nagyobb betűvel + a bemutatkozó apró betűvel + social linkek. A nav „Fotósok" innentől ide mutat (`/about#photographers` helyett; az About `#photographers` szekció mint „jelentkezz fotósnak" CTA megmarad). **Admin**: a fotós-részletnézet (`Admin/Photographers/Show.vue`) profil-űrlapja bővült — **profilkép feltöltés** (`ImageProcessingService::makeAvatar()` → 512×512 WebP a `public` diskre, `users.avatar_s3_key`; „Profilkép törlése" = `remove_avatar`) + **„Nyilvános profil" checkbox** (`is_public`). `PhotographerController::update` `is_public` most `required`, `avatar` nullable image. A demo seeder minden fotóst + az admint `is_public=true`-ra állít, egyedi bemutatkozókkal (placeholder). Tesztek: `tests/Feature/PublicPhotographersTest.php` (2).
  409 PHPUnit teszt zöld.

- Térkép-kereső (`EventMapModal.vue`) — a pinen az adott **helyszínen tartott fotózások (események) száma** látszik (`groupedLocations` a `location` szerint von össze), nem a médiaszám. A modal szűrői (dátum / fotós / típus) minden modalból indított navigációnál átmennek: `Events/Index.vue` `mapFilterParams()` + `Welcome.vue` pin-kattintáskor `submitSearch()`. Fotós-szűrés `photographer_id` = `users.id` **UUID**, ezért `$request->string()` (nem `integer()`). `/events` galéria: `per_page` választó (10/20/50/100/200, default 20). A térkép `live` **és** `announced` eseményeket is mutat — a pin **piros**, ha a helyszínen van már `live` fotózás (van megvásárolható kép), és **borostyán, szaggatott** keretű, ha csak előhirdetett („hamarosan"); a modal alján legend jelenik meg, ha van előhirdetett pin. A főoldali „Ország" statisztika ezzel szemben csak `live` eseményt számol (ezért lehet ott 1 ország, a térképen meg 2).
- Lógó szálak lezárva: (1) PWA elavult-chunk kezelés (`vite:preloadError` → egyszeri reload, `clientsClaim`), (2) dashboard „helyszín-feliratkozó" KPI valós adat (`EventSubscription` distinct email), (3) konverziós tölcsér valós top-of-funnel a saját `event_views` napi rollupból (nincs Plausible/külső analitika). 251 PHPUnit teszt zöld.

- Cloudflare R2 tároló (éles) kész — új Composer függőség (jóváhagyva): `league/flysystem-aws-s3-v3`. `config/filesystems.php`: `r2_public` (publikus bucket + `R2_PUBLIC_URL` domain/CDN) + `r2_private` (privát, csak `DownloadController` streameli), közös account-endpoint (`R2_ENDPOINT`), `use_path_style_endpoint: true`. `App\Services\MediaStorage` az EGYETLEN disknév-forrás: `public()` (`MEDIA_PUBLIC_DISK`, def. `public`), `archive()` (`MEDIA_ARCHIVE_DISK`, def. `nas`), `STAGING` = mindig `local`, `hasArchiveTier()` (`archive != local`), `diskForOriginal($media)`, `publicBaseUrl()`. `Media::STORAGE_NAS` érték változatlanul `'nas'` (= „az archív diskon van", nincs adatmigráció). `ArchiveMediaOriginalToNas` az `archive()` diskre ír (NAS eseten `NasConnection::applyRuntimeConfig`, R2 esetén nem), `hasArchiveTier()==false` → azonnal visszatér. `ProcessImage/VideoMedia`, `DownloadController`, `Admin\MediaController`, `AnalyzePendingPlates` (a nem-lokális eredetit temp fájlba tölti az Interventionhoz), `HeroSlideController`, `routes/web.php` hero — mind `MediaStorage`-ből. Frontend: `HandleInertiaRequests` `mediaBaseUrl` shared prop + `resources/js/Composables/useMediaUrl.js` (`mediaUrl` / `mediaUrlAbsolute`); ~12 Vue fájlban a `/storage/${key}` lecserélve. Átállás: `php artisan kanyarfotozas:sync-media-storage --from-public=public --from-archive=local [--dry-run]` — a meglévő fájlokat a beállított diskekre másolja + `original_storage`-t frissíti. `.env.example`: `MEDIA_*` + `R2_*` blokk. A `/admin/settings/storage` oldalon egy összecsukható **„Kezelési emlékeztető"** blokk foglalja össze az FTP/NAS ↔ R2 választást (`.env` 2 sor + sync parancs, hol vannak a kulcsok, költség). 259 PHPUnit teszt zöld.

- **CI (GitHub Actions)** kész — `.github/workflows/ci.yml`, `push` (main/master) + minden `pull_request`. 3 párhuzamos job:
  1. **phpunit** — PHP 8.4 (`shivammathur/setup-php`, `pdo_pgsql`/`pgsql`/`gd`/`exif`/`intl`/`zip`/`bcmath`), **`postgis/postgis:16-3.4` service** (`kanyarfotozas_test` DB), `apt-get install ffmpeg` (a videó-pipeline tesztekhez), `CREATE EXTENSION postgis` + `php artisan migrate --force` (séma smoke-teszt) + `php artisan test`. **FONTOS**: a `phpunit.xml` `<env>` bejegyzései nincsenek `force`-olva, ezért a job-szintű `env:` (`DB_PASSWORD: postgres` stb.) felülírja a bennük lévő helyi jelszót — ez ellenőrizve.
  2. **pint** — `vendor/bin/pint --test` (csak ellenőrzés, nem javít).
  3. **build** — Node 22, `npm ci`, `npm run build`.
  Composer letöltési cache (`~/.cache/composer`, kulcs: `composer.lock` hash) + npm cache. **A projekt még nincs git alatt** — a workflow a `git init` + GitHub remote + első push után aktiválódik. Ha a default branch nem `main`/`master`, a `push:` szűrőt igazítani kell.

- **Teljesítmény + akadálymentesítés (a11y)** kész — a publikus oldalak átvizsgálva:
  - **Fókusz**: globális `:focus-visible` körvonal a `resources/css/app.css`-ben (mindenhol látható billentyűzet-fókusz, még ott is, ahol a komponens `focus:outline-none`-t használ).
  - **`prefers-reduced-motion`**: globális CSS-reset (animációk/átmenetek ~0-ra) az app.css-ben (a hero már külön is kezelte JS-ből).
  - **Kontraszt**: a „default" (Aszfalt) séma sötét `--color-muted` `#666666` → **`#8a8a8a`** (WCAG AA ~5:1 a #0d0d0d-n) — `ThemeSettings::PRESETS` + app.css fallback. A többi séma muted-je már megfelelt.
  - **Skip-link**: „Ugrás a tartalomra" (`common.skip_to_content`) a `PublicLayout`-ban, `<main id="main" tabindex="-1">` célponttal; `aria-current="page"` a Public + Admin nav aktív elemén.
  - **Modálok**: `MediaLightbox` + `EventMapModal` — `role="dialog"` / `aria-modal` / `aria-label`, Esc zár, a lightbox fókuszt visz a modálra és záráskor visszaadja az előző elemnek. A lightbox nyilai `common.previous` / `common.next` (nem a nyers `→` karakter), a fő kép valódi `alt`.
  - **Képek**: `MediaCard` select-gomb `aria-label` (eddig nem volt hozzáférhető neve); `decoding="async"` a galéria- és borítóképeken; a hero első képe `fetchpriority="high"` + `loading="eager"`, a többi `lazy` (LCP).
  - Új lang kulcsok: `common.previous` / `common.next` / `common.skip_to_content` (hu+en).
  - **Deploy-jegyzet**: a `/build/*` (hash-elt) assetekre + a `public` disk médiára hosszú `Cache-Control` fejléc kell a webszerveren — ez nginx/apache config, a deploy-runbookba tartozik, nincs kódban.
  358 PHPUnit teszt zöld.

- **Kisebb csiszolások (indulás előtti „A" csomag)** kész:
  - **Főoldali statisztikák élővé téve** — a `/` route closure `stats` propot ad (`photos`/`videos` = `ready` `Media` típusonként, `locations`/`countries` = distinct a `live` eseményeken); `Welcome.vue` a propból számol (`Intl.NumberFormat('hu-HU')`), nem fix `0`.
  - **`robots.txt` javítás** — `SitemapController::robots()`: `Disallow: /photographer` → `Disallow: /photographer/` (a prefix-tiltás blokkolta a publikus `/photographers` oldalt!), `+ Disallow: /organizer/`, `+` egy komment-sor a `/llms.txt`-re.
  - **Publikus hibaoldalak** — `resources/js/Pages/Error.vue` (PublicLayout, 403/404/419/429/500/503, lokalizált `errors.*` kulcsok hu+en). Két útvonal: (1) **nem létező URL** → `Route::fallback()` a `web.php` végén (a teljes `web` middleware-t megkapja → `SetLocale` + `HandleInertiaRequests`, így a hibaoldal is fordított + márkázott); (2) **matched route-ból dobott** 403/404/419/429/500/503 → `bootstrap/app.php` `withExceptions` `$exceptions->respond(...)` (nem-JSON, nem-Inertia böngészőkérésre `Inertia::render('Error')` a megfelelő státuszkóddal; az 500/503-at helyi `local` környezetben NEM veszi át — marad a részletes hibalap). **Fontos**: a `respond()` handler NEM állít shared propokat, ezért az unmatched-404-hez a fallback route kell (különben a hibaoldal nyers fordítási kulcsokat mutat).
  - **`/about` `#photographers`** — a szekció megmarad mint „jelentkezz fotósnak" CTA, de a gomb a `/photographers` publikus oldalra mutat (a régi `/contact` helyett).

- **Barion Smart Gateway v2** kész — harmadik fizetési szolgáltató a Stripe/SimplePay mellé, a `PaymentGateway` absztrakción keresztül, **ki/be kapcsolható** (`payment_enabled:barion`). `App\Services\Payments\BarionGateway`: `POST {base}/v2/Payment/Start` (JSON, `POSKey` a testben) → `GatewayUrl` redirect; **nincs HMAC-aláírás** — a biztonság alapja, hogy a `POSKey` titkos, és a fizetés állapotát MINDIG a saját szerverünkről kérdezzük vissza (`GET /v2/Payment/GetPaymentState`). Visszatérés (élmény): `GET /checkout/barion/return?paymentId=` (`CheckoutController::barionReturn`). Callback (megbízható): `POST|GET /api/barion/callback` (`BarionCallbackController`, `routes/api.php`, `payment.settings` middleware, nincs CSRF) — `Succeeded` állapotnál `markPaid`. Visszatérítés: `POST /v2/Payment/Refund` (a belső `TransactionId`-t a `GetPaymentState`-ből szedi). Beállítás: `/admin/settings/critical` „Barion" fieldset + `App\Services\PaymentSettings` (`site_settings`: `barion_pos_key` **Crypt-titkosítva**, `barion_payee` = a Barion fiók e-mailje, `barion_sandbox`). `config/services.php` `barion` blokk + `.env.example` `BARION_POS_KEY`/`BARION_PAYEE`/`BARION_SANDBOX`. `orders.payment_provider` enum már EPIC-02 óta tartalmazza a `barion`-t (nincs migráció). `SystemReadiness::payments()` jelzi a Barion állapotát + éles-sandbox figyelmeztetést. Teszt: `tests/Feature/Public/BarionCheckoutTest.php` (5).
  418 PHPUnit teszt zöld.

- **Kézbesítési gyorsítótár (delivery cache) + letöltés-hibakezelés** kész — a megvásárolt letölthető fájlok (fotó JPEG+WebP, videó MP4) a fizetés után átmásolódnak az archív rétegről (NAS SFTP / R2 privát) egy gyors, mindig elérhető lokális `delivery` diskre, így a **vásárló-oldali letöltés független az archív elérhetőségétől**.
  - `App\Services\OrderFulfillment` — `prepare(Order)` (másolás archív→delivery, sose dob, beállítja a `fulfillment_*` mezőket), `resolveSource(Order, Media, $format)` (`[disk, key]`: előbb a delivery gyorsítótár, fallback az archív, `null` ha sehol), `purge(Order)`. `deliveryKey` = `orders/{order}/{media}.{ext}`. A **forensic jelet NEM** ide sütjük — a gyorsítótár a tiszta fájlt tárolja, a per-vásárló jel a kiszolgáláskor, memóriában kerül rá.
  - `orders` új oszlopok: `fulfillment_status` (pending|ready|failed), `fulfillment_prepared_at`, `fulfillment_attempts`, `fulfillment_error`.
  - `App\Jobs\PrepareOrderDownloads` — a `CheckoutService::markPaid` diszpécseli. Sose dob (a fizetés lezárását nem akaszthatja meg). Elakadt archívnál egyre ritkábban újrapróbál (perc→óra), 8 próba után `failed` + `ProactiveAlertDigestMail` a superadminoknak. **Sync queue-nál** (nincs külön worker) nem rekurzál — az ütemezett `kanyarfotozas:retry-order-fulfillment` (15 perc) próbálkozik.
  - `kanyarfotozas:purge-delivery-cache` (óránként) — a lejárt / kimerült letöltési tokenű (72h / 5 megnyitás) + a `media.delivery_max_age_hours`-nál (alap 168h) régebbi rendelések gyorsítótárát törli. Teljes visszatérítéskor a `CheckoutService::refund` is `purge`-öl.
  - `DownloadController` a `resolveSource`-ból szolgál ki; a `Download/Show` oldal `preparing` flaggel jelzi, ha még másol (+ egy szinkron `rescue(prepare())` lökés, ha a worker nem futna); minden hiányzó fájl **404** (nem 500).
  - `App\Services\ProactiveAlerts::failedFulfillment()` — dashboard-riasztás a `failed` állapotú fizetett rendelésekről.
  - **`MediaDeleter`**: fizetett rendeléssel hivatkozott médiát **nem töröl véglegesen** (`MediaHasSalesException`) — a `MediaController::destroy`/`bulkDestroy` „rejtsd el helyette" hibát ad; `delete(..., force: true)` felülbírálja. `CheckoutService::createPendingOrder` már eleve csak `ready` státuszú médiát enged a kosárba.
  - `.env`: `MEDIA_DELIVERY_DISK` (alap `delivery`, lokális), `MEDIA_DELIVERY_MAX_AGE_HOURS`. `config/filesystems.php` `delivery` disk (`storage/app/delivery`).
  - Tesztben: a base `Tests\TestCase::setUp` globálisan `Storage::fake('delivery')` (a fizetést lezáró/letöltő tesztek ide másolnak). Teszt: `tests/Feature/DeliveryCacheTest.php` (7).
  425 PHPUnit teszt zöld.

- **E-mail küldés (SMTP) az adminból** kész — eddig az e-mail volt az egyetlen kritikus külső hozzáférés, amit csak a szerver `.env`-jét szerkesztve lehetett beállítani; most a fizetési kulcsok mintájára a `/admin/settings/critical` „E-mail küldés" szekciójában állítható. `App\Services\MailSettings` (`site_settings`: `mail_mailer` [smtp|log], `mail_host`, `mail_port`, `mail_encryption` [tls|ssl|none → `mail.mailers.smtp.scheme`], `mail_username`, `mail_password` **Crypt-titkosítva**, `mail_from_address`, `mail_from_name`; `.env`/config fallback). `applyRuntimeConfig()` az **`AppServiceProvider::boot()`-ból** fut (`rescue`-ban) → web + queue worker + cron egységesen; `update()` után `mail.manager` `forgetMailers()`. **„Tesztlevél küldése"** gomb (`POST /admin/settings/critical/mail/test`, `throttle:6,1`, `Mail::raw`). A `SystemReadiness` „E-mail" csoportja kapott egy `#mail` linket. Mivel a `mail_from_address` innentől a `site_settings`-ben él, az „oldal átnevezése" (`SiteIdentity`) automatikusan átírja domainváltáskor (nincs kód-módosítás). `.env`: a `MAIL_*` értékek fallbackként maradnak. **Válasz (Reply-To) cím** is beállítható (`mail_reply_to`): a `ContactReplyMail` + `ContactConfirmationMail` `envelope`-ja ezt teszi Reply-To-nak (`MailSettings::replyTo()`), így a vevő „Válasz"-a egy figyelt postafiókba megy, nem a `noreply@`-ra. Teszt: `tests/Feature/MailSettingsTest.php` (6).
  431 PHPUnit teszt zöld.

- **Jogi oldalak (ÁSZF + Impresszum) + pénztár-hozzájárulás** kész — magyar webshop-kötelező elemek.
  - `App\Services\LegalPages` — az Impresszum + ÁSZF tartalma **Markdownban**, a `site_settings`-ben (`legal_impressum` / `legal_terms` / `legal_terms_updated_at`). A publikus oldal `Str::markdown($md, ['html_input' => 'strip', 'allow_unsafe_links' => false])`-szal renderel (nyers HTML kiszűrve). Defaultként HU vázak `[kitöltendő]` jelölőkkel (üres oldal helyett) — az `impressumMarkdown()`/`termsMarkdown()` adja a mentett VAGY a default értéket.
  - **Admin**: `/admin/settings/legal` (superadmin, „Jogi oldalak" nav) — `Admin\LegalSettingsController` + `Admin/Settings/Legal.vue`: 2 textarea (Markdown), a `forForm()` a vázzal tölti elő, „alapértelmezett váz" jelölő + `terms_updated_at`.
  - **Publikus**: `GET /impresszum` → `Info/Impressum.vue`, `GET /aszf` → `Info/Terms.vue` (`PageController::impressum`/`terms`); a `PublicLayout` láblécében linkek (`footer.impressum` / `footer.terms`), a sitemapban is.
  - **Pénztár-hozzájárulás**: a `Cart/Index.vue`-ban egy **kötelező** checkbox — az ÁSZF + adatvédelmi tájékoztató elfogadása ÉS a 14 napos elállási jog megszűnésének tudomásulvétele (azonnali digitális teljesítés, 45/2014. Korm. r. 29. § m). A gomb `:disabled` a pipa nélkül. Backend: `CheckoutController::store` `'terms_accepted' => ['accepted']`; `CheckoutService::createPendingOrder` `orders.terms_accepted_at = now()` (bizonyíték). Új oszlop: `orders.terms_accepted_at`.
  - Tesztek: `tests/Feature/LegalPagesTest.php` (4) + `CheckoutFlowTest` „requires accepting the terms". A meglévő checkout-tesztekbe (`SimplePay`/`Barion`/`Invoicing`/`PlateRecognition`) bekerült a `'terms_accepted' => true`.
  436 PHPUnit teszt zöld.

- **Közösségi média linkek (admin)** kész — `App\Services\SocialLinks` (`site_settings`: `social_facebook` / `social_instagram` / `social_youtube` / `social_tiktok`, `normalize()` az `https://`-t elé teszi). Admin: `/admin/settings/social` (superadmin, „Közösségi média" nav) — `Admin\SocialSettingsController` + `Admin/Settings/Social.vue`, 4 URL-mező. A `HandleInertiaRequests` `social` shared prop (`SocialLinks::all()` — csak a kitöltöttek, fix sorrend, `{platform, label, abbr, url}`). Publikus: a `PublicLayout` lábléce (FB/IG/YT/TT rövidítés, `target=_blank rel=noopener`, üres platform elrejtve) + a `Info/Contact.vue` „Kövess minket" szekció. Teszt: `tests/Feature/SocialLinksTest.php` (4).
  A megjelenítés `resources/js/Components/SocialIcon.vue` **márka-ikonokkal** (simple-icons path, `currentColor`): a Kapcsolat oldal 2 oszlopban az ikon + a séma nélküli rövid link (`SocialLinks::all()` `display` mező), a lábléc csak az ikonok. Üres mező → sehol nem jelenik meg.
  440 PHPUnit teszt zöld.

- **Kisebb tartalmi módosítások** — a publikus fejléc-navba bekerült a **GY.I.K.** (`nav.faq`, `/faq`, Árak és Rólunk közé). A „Fotós vagyok — hogyan árulhatom itt a képeimet?" GYIK-válasz átfogalmazva (kapcsolatfelvétel-alapú); a `FaqItemSeeder`-ben ÉS a dev DB-ben is frissítve (nincs admin FAQ-szerkesztő — a `FaqItem` csak a seederből jön).

- **Publikus űrlap-védelem (`App\Support\FormGuard`)** kész — a régi session-alapú `ArithmeticCaptcha` **törölve**, helyette egy **stateless, aláírt challenge** több réteggel, külső szolgáltatás nélkül:
  1. **Time-trap** — a token aláírt kiadási időbélyeget hordoz; a `formguard.min_fill_seconds`-nél (alap 3) gyorsabb beküldés bot.
  2. **Számtani kérdés** — a látogató kézzel válaszol (a helyes választ a token aláírja, `guard_answer` mező, 1 sorba rendezve).
  3. **Proof-of-work** — a böngésző egy SHA-256 hashcash puzzle-t old meg (`formguard.pow_bits`, alap 17 ≈ ~130k hash, láthatatlan ~0,1–1 s). Kliens: `resources/js/Support/sha256.js` (kompakt, dep-mentes) + `resources/js/Composables/useFormGuard.js` (a puzzle-t a háttérben oldja, watch a challenge cseréjére). A `guard_token` + `guard_pow` submitkor kerül a formba.
  4. **Replay-védelem** — egy megoldott token `n` (nonce) csak egyszer használható (`Cache`, TTL = token élettartam).
  5. **Erősített honeypot** — 2 mező (`website` + `nickname`), CSS-sel a képernyőn kívül (nem `display:none`). Bármelyik kitöltve → `isBot()` → „csendben sikeres" válasz.
  6. **Rétegzett rate limit** — `RateLimiter::for('contact')` (`AppServiceProvider`): IP 3/perc + e-mail-cím 5/óra + globális 60/óra. A `/contact` és `/media/{media}/question` route-on `throttle:contact`.
  - Token = `base64url(payload).base64url(HMAC-SHA256(payload, APP_KEY))`. Challenge: `GET /contact` → `guard` prop, `GET /media/{id}` → `contactGuard` prop. Lejárt/elhasznált challenge-nél a Vue `router.reload({ only: ['guard'] })`-del kér frisset.
  - `config/formguard.php` + `.env` `FORMGUARD_POW_BITS` / `FORMGUARD_MIN_FILL_SECONDS`. Teszt: `phpunit.xml` `FORMGUARD_POW_BITS=8` (gyors). `FormGuard::solveProofOfWork()` a PHP-s megoldó (tesztekhez). A tesztek `$this->travel(5)->seconds()`-szal lépik át a time-trapet.
  - Tesztek: `tests/Feature/Public/ContactFormTest.php` (valid / rossz válasz / time-trap / rossz PoW / replay / tamperelt token / honeypot) + `MessageManagerTest` média-kérdés.

- **hCaptcha (opcionális, adminból kapcsolható)** — a superadmin a `/admin/settings/critical` „Captcha — hCaptcha" szekcióban bekapcsolhatja: ekkor a Kapcsolat űrlapon (+ a média-oldali „Kérdés a fotóshoz"-nál) a hCaptcha widget **VÁLTJA** a FormGuard számtani kérdését; a többi réteg (time-trap, PoW, honeypot, replay, rate limit) marad. `App\Services\CaptchaSettings` (`site_settings`: `hcaptcha_enabled` / `hcaptcha_site_key` / `hcaptcha_secret` **Crypt-titkosítva**, `.env`/config fallback: `HCAPTCHA_*`). `enabled()` = toggle ON ÉS site key ÉS secret. `verify()` → `POST https://api.hcaptcha.com/siteverify` (`rescue`-ban, hiba = false). Backend: `FormGuard::verify(Request, skipArithmetic: $hcaptcha)` — bekapcsolt hCaptchánál a controller külön ellenőrzi a `h-captcha-response` tokent (`hcaptcha` validációs kulcs), a FormGuard kihagyja a számtani ellenőrzést. Frontend: `resources/js/Composables/useHcaptcha.js` (dinamikusan tölti a `js.hcaptcha.com/1/api.js?render=explicit`-et, `hcaptcha.render()` explicit módban, remount-biztos), a `Contact.vue` / `Media/Show.vue` `v-if="hcNeeded"` a widget, különben a számtani mező. Teszt-kulcspár (mindig sikeres) a `.env.example`-ben. Teszt: `tests/Feature/HcaptchaTest.php` (6).
  449 PHPUnit teszt zöld.

## Jövőbeli ötletek (később — NE valósítsd meg, amíg a felhasználó nem kéri)
Bevétel-növelő funkciók, a felhasználó szándékosan elhalasztotta:
- **„Találd meg magad" — rajtszám / arc alapú keresés**: a vásárló beírja a rajtszámát (start number), és megkapja az összes róla készült képet. A meglévő rendszámfelismerő (Plate Recognizer) infrastruktúrára építhető (OCR a rajtszám-táblán / arcfelismerés). Motorsportban ez a legnagyobb konverzió-növelő — a vásárló nem tud 800 kép közt végignézni.
- **„Minden kép rólam" csomagár**: egy kattintással a vásárló összes (rajtszám/arc alapján hozzárendelt) képét a kosárba teszi egy csomag-kedvezménnyel. Jól illeszkedik a már kész esemény-szintű árazáshoz.
- **Print-on-demand (poszter / vászonkép)**: külső nyomdai partner a fizetési absztrakció mögött, extra árrés. A vásárló a letöltés helyett/mellett fizikai terméket is rendelhet.

## Fontos helyi környezeti fix-ek (ne írd felül véletlenül)
- `C:\Users\bz\.config\herd\bin\php84\php.ini`: `upload_tmp_dir`, `upload_max_filesize=512M`, `post_max_size=4096M`, `max_file_uploads=200`, `memory_limit=512M`, `max_execution_time=300` — a batch média feltöltéshez kellett, a `php artisan serve` beépített szervere enélkül "unable to create temporary file" hibát dobott
- `.claude/launch.json` indítja a `php artisan serve`-et a Browser panelhez

<laravel-boost-guidelines>
=== foundation rules ===

# Laravel Boost Guidelines

The Laravel Boost guidelines are specifically curated by Laravel maintainers for this application. These guidelines should be followed closely to ensure the best experience when building Laravel applications.

## Foundational Context

This application is a Laravel application running on PHP 8.4. You are an expert with the Laravel ecosystem. Always use the APIs that match the installed major version of each package — do not assume a version.

Before relying on a package's API, confirm its installed version:
- PHP packages: run `composer show --direct` to list direct dependencies with versions, or `composer show <vendor/package>` for a single package.
- JS packages: check `package.json` for the installed versions.

## Conventions

- You must follow all existing code conventions used in this application. When creating or editing a file, check sibling files for the correct structure, approach, and naming.
- Use descriptive names for variables and methods. For example, `isRegisteredForDiscounts`, not `discount()`.
- Check for existing components to reuse before writing a new one.

## Verification Scripts

- Do not create verification scripts or tinker when tests cover that functionality and prove they work. Unit and feature tests are more important.

## Application Structure & Architecture

- Stick to existing directory structure; don't create new base folders without approval.
- Do not change the application's dependencies without approval.

## Frontend Bundling

- If the user doesn't see a frontend change reflected in the UI, it could mean they need to run `npm run build`, `npm run dev`, or `composer run dev`. Ask them.

## Documentation Files

- You must only create documentation files if explicitly requested by the user.

## Replies

- Be concise in your explanations - focus on what's important rather than explaining obvious details.

=== boost rules ===

# Laravel Boost

## Project Rules

- This project contains committed, area-grouped rules in `.ai/rules` when that directory exists (settled decisions, non-obvious traps, standing constraints). Framework and package guidelines that only apply to specific paths (testing, frontend, components) also live there, under `.ai/rules/boost` — this is not just recorded decisions, it is load-bearing guidance you have not seen inline. Before you enter plan mode or create/edit any file, you MUST first: open @.ai/rules/index.md (it maps file globs to rule files), read every rule file whose globs cover the path(s) in scope, and run `grep -rin 'keyword' .ai/rules` to catch what a path match alone misses. Do not write code until you have read and are following every matching rule. If `.ai/rules` does not exist, continue without it.

## Artisan

- Run Artisan commands directly via the command line (e.g., `php artisan route:list`). Use `php artisan list` to discover available commands and `php artisan [command] --help` to check parameters.
- Inspect routes with `php artisan route:list`. Filter with: `--method=GET`, `--name=users`, `--path=api`, `--except-vendor`, `--only-vendor`.
- Read configuration values using dot notation: `php artisan config:show app.name`, `php artisan config:show database.default`. Or read config files directly from the `config/` directory.

## Tinker

- Execute PHP in app context for debugging and testing code. Do not create models without user approval, prefer tests with factories instead. Prefer existing Artisan commands over custom tinker code.
- Always use single quotes to prevent shell expansion: `php artisan tinker --execute 'Your::code();'`
  - Double quotes for PHP strings inside: `php artisan tinker --execute 'User::where("active", true)->count();'`

=== php rules ===

# PHP

- Always use curly braces for control structures, even for single-line bodies.
- Use PHP 8 constructor property promotion: `public function __construct(public GitHub $github) { }`. Do not leave empty zero-parameter `__construct()` methods unless the constructor is private.
- Use explicit return type declarations and type hints for all method parameters: `function isAccessible(User $user, ?string $path = null): bool`
- Use TitleCase for Enum keys: `FavoritePerson`, `BestLake`, `Monthly`.
- Prefer PHPDoc blocks over inline comments. Only add inline comments for exceptionally complex logic.
- Use array shape type definitions in PHPDoc blocks.

=== deployments rules ===

# Deployment

- Laravel can be deployed using [Laravel Cloud](https://cloud.laravel.com/), which is the fastest way to deploy and scale production Laravel applications.

=== inertia-laravel/core rules ===

# Inertia

- Inertia creates fully client-side rendered SPAs without modern SPA complexity, leveraging existing server-side patterns.
- Components live in `resources/js/Pages` (unless specified in `vite.config.js`). Use `Inertia::render()` for server-side routing instead of Blade views.
- ALWAYS use `search-docs` tool for version-specific Inertia documentation and updated code examples.
- IMPORTANT: Activate `inertia-vue-development` when working with Inertia Vue client-side patterns.

# Inertia v3

- Use all Inertia features from v1, v2, and v3. Check the documentation before making changes to ensure the correct approach.
- New v3 features: standalone HTTP requests (`useHttp` hook), optimistic updates with automatic rollback, layout props (`useLayoutProps` hook), instant visits, simplified SSR via `@inertiajs/vite` plugin, custom exception handling for error pages.
- Carried over from v2: deferred props, infinite scroll, merging props, polling, prefetching, once props, flash data.
- When using deferred props, add an empty state with a pulsing or animated skeleton.
- Axios has been removed. Use the built-in XHR client with interceptors, or install Axios separately if needed.
- `Inertia::lazy()` / `LazyProp` has been removed. Use `Inertia::optional()` instead.
- Prop types (`Inertia::optional()`, `Inertia::defer()`, `Inertia::merge()`) work inside nested arrays with dot-notation paths.
- SSR works automatically in Vite dev mode with `@inertiajs/vite` - no separate Node.js server needed during development.
- Event renames: `invalid` is now `httpException`, `exception` is now `networkError`.
- `router.cancel()` replaced by `router.cancelAll()`.
- The `future` configuration namespace has been removed - all v2 future options are now always enabled.

=== laravel/core rules ===

# Do Things the Laravel Way

- Use `php artisan make:` commands to create new files (i.e. migrations, controllers, models, etc.). You can list available Artisan commands using `php artisan list` and check their parameters with `php artisan [command] --help`.
- If you're creating a generic PHP class, use `php artisan make:class`.
- Pass `--no-interaction` to all Artisan commands to ensure they work without user input. You should also pass the correct `--options` to ensure correct behavior.

### Model Creation

- When creating new models, create useful factories and seeders for them too. Ask the user if they need any other things, using `php artisan make:model --help` to check the available options.

## APIs & Eloquent Resources

- For APIs, default to using Eloquent API Resources and API versioning unless existing API routes do not, then you should follow existing application convention.

## URL Generation

- When generating links to other pages, prefer named routes and the `route()` function.

## Testing

- When creating models for tests, use the factories for the models. Check if the factory has custom states that can be used before manually setting up the model.
- Faker: Use methods such as `$this->faker->word()` or `fake()->randomDigit()`. Follow existing conventions whether to use `$this->faker` or `fake()`.
- When creating tests, make use of `php artisan make:test [options] {name}` to create a feature test, and pass `--unit` to create a unit test. Most tests should be feature tests.

## Vite Error

- If you receive an "Illuminate\Foundation\ViteException: Unable to locate file in Vite manifest" error, you can run `npm run build` or ask the user to run `npm run dev` or `composer run dev`.

=== pint/core rules ===

# Laravel Pint Code Formatter

- If you have modified any PHP files, you must run `vendor/bin/pint --dirty --format agent` before finalizing changes to ensure your code matches the project's expected style.
- Do not run `vendor/bin/pint --test --format agent`, simply run `vendor/bin/pint --format agent` to fix any formatting issues.

=== phpunit/core rules ===

# PHPUnit

- This project uses PHPUnit. Create tests with `php artisan make:test --phpunit {name}`.
- Do not include the test suite directory in `{name}`. Use `SomeFeatureTest`, not `Feature/SomeFeatureTest`.
- Read the `testing-best-practices` skill for guidance on coverage, naming, structure, dependency isolation, and review.

## Running Tests

- Run the narrowest set of tests that covers the change. Pass a file path or `--filter=testName` to `php artisan test --compact`.
- Rerun a test after each change to it.
- Run `vendor/bin/phpunit` to call the test runner directly. It accepts the same file path and `--filter=testName` arguments.

=== inertia-vue/core rules ===

# Inertia + Vue

Vue components must have a single root element.
- IMPORTANT: Activate `inertia-vue-development` when working with Inertia Vue client-side patterns.

</laravel-boost-guidelines>
